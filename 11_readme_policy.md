# 폴더별 readme.md 작성 정책

## 목적

각 폴더의 `readme.md`는 해당 폴더 안의 하위 폴더와 파일들이 어떤 내용을 가지고 있는지 빠르게 파악하기 위한 가이드 문서다.

설계 단계의 상세 논의는 루트의 번호별 설계 문서에서 관리한다.

## 작성 언어

모든 `readme.md`는 한글로 작성한다.

## 작성 대상

코드를 추가하거나 수정한 폴더에는 `readme.md`를 작성하거나 갱신한다.

하위 폴더가 많은 경우, 필요한 깊이까지 각 폴더별로 작성한다.

예:

```text
playwright_job/readme.md
playwright_job/management/readme.md
playwright_job/management/commands/readme.md
main/readme.md
main/templates/readme.md
main/static/readme.md
```

## 작성 범위

`readme.md`에는 아래 내용을 담는다.

1. 폴더의 역할
2. 주요 파일 설명
3. 하위 폴더 설명
4. 이 폴더에서 주의할 점
5. 관련 루트 설계 문서 링크

## 작성하지 않을 내용

아래 내용은 폴더별 `readme.md`에 길게 쓰지 않는다.

- 전체 시스템 설계 논의
- 아직 확정되지 않은 아이디어
- 긴 회의 기록
- 검사 규칙의 상세 목록
- 운영 정책 전체

이런 내용은 루트 설계 문서에서 관리한다.

## 기본 템플릿

```markdown
# 폴더명

## 역할

이 폴더가 담당하는 기능을 짧게 설명한다.

## 주요 파일

| 파일 | 설명 |
| --- | --- |
| example.py | 파일의 역할 |

## 하위 폴더

| 폴더 | 설명 |
| --- | --- |
| subfolder | 하위 폴더의 역할 |

## 주의사항

- 이 폴더를 수정할 때 주의할 점을 적는다.

## 관련 문서

- `01_automation_flow.md`
```

## 갱신 규칙

코드 변경 시 아래 질문을 확인한다.

1. 새 파일을 추가했는가?
2. 기존 파일의 역할이 바뀌었는가?
3. 하위 폴더를 추가했는가?
4. 폴더 사용 시 주의할 점이 생겼는가?
5. 관련 루트 설계 문서가 바뀌었는가?

하나라도 해당하면 해당 폴더의 `readme.md`를 갱신한다.

## 파일명 표기

프로젝트에서는 사용자가 요청한 이름인 `readme.md`를 사용한다.

대소문자를 통일하기 위해 새로 만드는 문서는 소문자 `readme.md`로 작성한다.

## 루트 설계 문서와의 관계

루트의 번호별 설계 문서:

```text
01_automation_flow.md
02_database_design.md
...
```

위 문서들은 시스템 설계와 정책을 설명한다.

각 폴더의 `readme.md`는 실제 폴더와 파일 구조를 설명한다.

따라서 설계가 바뀌면 루트 설계 문서를 먼저 갱신하고, 코드 구조가 바뀌면 관련 폴더의 `readme.md`를 갱신한다.
