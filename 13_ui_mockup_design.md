# UI 목업 설계

## 목적

실제 DB/API 구현 전에 mock 데이터를 사용해 프로젝트 선택, 진행상황, 결과 조회 화면의 동선과 정보를 자세히 검토한다.

## 방식

B 방식으로 진행한다.

```text
정적 화면이 아니라 mock 데이터 기반 프론트 목업을 만든다.
버튼, 탭, 필터, 상태 변화 등 사용감을 확인할 수 있게 한다.
실제 DB/API 연결은 이후 단계에서 붙인다.
```

## UI 검토 원칙

- 실제 업무자가 반복해서 사용할 수 있는 밀도 있는 화면으로 만든다.
- 랜딩 페이지가 아니라 바로 작업 화면이 첫 화면이어야 한다.
- 장기 작업 특성상 현재 작업 상태와 잠금 상태가 항상 보여야 한다.
- 위험 동작은 명확하게 구분한다.
- 화면 문구는 기능 설명보다 현재 상태와 다음 행동을 알려주는 데 집중한다.
- 카드형 홍보 페이지처럼 만들지 않고 업무용 대시보드처럼 구성한다.

## 전체 레이아웃 초안

단일 업무 화면 안에서 탭으로 전환한다.

```text
상단 바
├─ 시스템명
├─ worker 상태
├─ 현재 active job 상태

본문
├─ 탭: 프로젝트 선택
├─ 탭: 진행상황
└─ 탭: 결과 조회
```

## 상단 바

항상 표시할 정보:

- 시스템명: ECM 제출물 자동 검사
- worker 상태: 실행 중 / 중지 / heartbeat 지연
- 현재 작업 상태: 작업 없음 / 진행 중 / 실패 / 완료
- 마지막 갱신 시각

상태 색상 초안:

- 정상: 녹색
- 진행 중: 파란색
- 경고: 주황색
- 오류: 빨간색
- 정보 없음: 회색

## 탭 1: 프로젝트 선택

### 목적

`ecmlist.db`의 `ecm_list` 목록에서 사용자가 다운로드/검사할 프로젝트를 선택한다.

### 상단 필터 영역

필터 후보:

- 프로젝트번호
- 인증일자 범위
- 회사명
- 제품명
- 시험PL
- 점검결과

초기 목업에서는 아래 필터를 우선 제공한다.

```text
프로젝트번호 검색
회사명 검색
제품명 검색
시험PL 선택
점검결과 선택
```

### 프로젝트 목록 테이블

기본 컬럼:

- 선택 체크박스
- 프로젝트번호
- 인증일자
- 회사명
- 제품명
- 시험PL
- 점검결과

추가 확인용 컬럼 후보:

- 번호
- 계약서
- 시험성적서(PDF)
- SW저작권확인서

초기 목업에서는 기본 컬럼을 우선 표시하고, 행 확장 또는 상세 패널에서 전체 컬럼을 볼 수 있게 한다.

### 선택 요약 영역

표시 정보:

- 선택된 프로젝트 수
- 선택된 프로젝트번호 목록
- 작업 요청 버튼
- 선택 초기화 버튼

작업 요청 버튼 정책:

- active job이 없을 때만 활성화
- active job이 있으면 비활성화하고 안내 문구 표시

안내 문구:

```text
현재 다른 작업이 진행 중입니다. 완료 후 다시 요청해 주세요.
```

### 행 상세 보기

프로젝트 행 클릭 시 오른쪽 또는 하단에 상세 패널을 표시한다.

상세 패널에는 `ecm` 전체 컬럼을 보여준다.

목적:

- 사용자가 선택 전 프로젝트 정보를 확인할 수 있게 한다.
- 다운로드 대상 관련 컬럼이 비어 있는지 검토할 수 있게 한다.

## 탭 2: 진행상황

### 목적

현재 실행 중인 작업과 프로젝트별 진행 상태를 확인한다.

### 작업 요약

표시 정보:

- 작업 ID
- 작업 상태
- 요청 시각
- 시작 시각
- 전체 프로젝트 수
- 완료 수
- 실패 수
- 현재 처리 중 프로젝트번호
- 현재 단계 메시지

### 진행률

프로젝트 단위 진행률을 표시한다.

예:

```text
3 / 10 프로젝트 처리 완료
```

### 프로젝트별 진행 테이블

컬럼:

- 순번
- 프로젝트번호
- 회사명
- 제품명
- 상태
- 현재 단계
- 재시도 횟수
- zip 파일명
- 오류 메시지

상태 후보:

- 대기
- 진행 중
- 다운로드 완료
- 검사 대기
- 완료
- 실패
- 건너뜀

### 현재 단계 메시지 후보

```text
프로젝트 폴더 선택 중
문서 목록 전체 선택 중
파일 다운로드 메뉴 실행 중
폴더 선택 팝업 대기 중
다운로드 폴더 선택 중
전송현황 대기 중
zip 파일 확인 중
다운로드 완료
실패
```

### worker 상태 영역

표시 정보:

- worker 실행 여부
- worker PID
- 마지막 heartbeat
- stale 여부

문제 상태 문구:

```text
작업 실행기의 heartbeat가 지연되고 있습니다.
작업 실행기가 동작하지 않습니다. 관리자에게 문의하세요.
```

## 탭 3: 결과 조회

### 목적

완료된 작업과 프로젝트별 다운로드 결과를 조회한다.

검사 규칙 상세 결과는 마지막 단계에서 확장한다.

### 작업 목록

컬럼:

- 작업 ID
- 요청 시각
- 완료 시각
- 전체 프로젝트 수
- 성공 수
- 실패 수
- 상태

### 작업 상세

작업 선택 시 프로젝트별 결과를 표시한다.

컬럼:

- 프로젝트번호
- 회사명
- 제품명
- 상태
- zip 파일명
- zip 경로
- 실패 단계
- 오류 메시지

### 결과 필터

초기 필터:

- 전체
- 성공
- 실패

추후 검사 규칙 추가 후 확장:

- 규칙 실패만 보기
- 경고만 보기
- 파일별 보기

## mock 데이터 구성

목업에는 아래 상황이 모두 포함되어야 한다.

1. 작업이 없는 상태
2. 작업 진행 중 상태
3. worker heartbeat 지연 상태
4. 일부 프로젝트 성공, 일부 실패 상태
5. active job 때문에 새 요청이 차단되는 상태
6. 시스템 알림 중복 파일로 재시도된 프로젝트
7. zip 파일이 없는 실패 프로젝트

## 목업 구현 범위

초기 UI 목업은 실제 API 연결 없이 프론트에서 mock 데이터를 사용한다.

구현 후보:

- 기존 Django 템플릿 + 정적 JS
- `main/templates` 아래 새 화면
- `main/static/scripts` 아래 mock UI 스크립트
- `main/static/css` 아래 목업 전용 CSS

목업 화면 URL 후보:

```text
/download-review/
```

## 검토 포인트

사용자는 목업에서 아래 내용을 검토한다.

- 프로젝트 목록에서 필요한 정보가 충분한지
- 필터가 실제 업무 흐름에 맞는지
- 선택한 프로젝트 확인 방식이 편한지
- 진행상황 메시지가 이해되는지
- 실패한 프로젝트를 찾기 쉬운지
- 결과 조회 화면에서 필요한 정보가 보이는지
- 버튼 위치와 비활성화 정책이 안전한지

## 목업 완료 기준

- 실제 DB/API 없이도 주요 화면 3개를 전환할 수 있다.
- mock 데이터로 정상/진행/실패/잠금 상태를 모두 확인할 수 있다.
- 사용자가 디자인과 업무 동선을 검토할 수 있다.
- 검토 의견을 반영해 문서와 목업을 수정할 수 있다.
