# Python
__pycache__/
*.py[cod]
*.sqlite3

# Virtual Envs
.venv/
local_review_app/build/
local_review_app/dist/
venv/
env/
django/

# VS Code
.vscode/

# Django secret
*.log
*.pot
*.pyc

# OS
.DS_Store
Thumbs.db

# Migrations (선택)
*/migrations/__pycache__/
*/migrations/*.py[cod]

# 임베딩 캐시
main/data/*
!main/data/README.md
!main/data/prdinfo.xlsx
!main/data/security.xlsx
# reference.xlsx: weekly 동기화가 매일 갱신하는 로컬 산출물 — 더 이상 git 추적하지 않음
main/data/reference.xlsx
!main/data/ecmlist.db
!main/data/ecmlist2.db
main/data/workflow.db
main/data/ecm_agent.lock
main/data/faiss_bge_m3_ko.idmap.index
main/data/ngram_table.npz

# Static files
/staticfiles/

# Local tooling
.tmp/
run/

# 비밀 환경변수 설정 스크립트(실제 값 포함) — 커밋 금지.
# env.ps1 은 비밀값이 없어 추적 대상. set-secrets.ps1(실제 값)만 무시하고,
# set-secrets.ps1.example(플레이스홀더 템플릿)은 커밋한다.
set-secrets.ps1

# Local Google OAuth credentials
main/utils/ecmList/credentials.json
main/utils/ecmList/token.json

# Setup 바이너리 (setup.ps1이 자동 다운로드)
setup/*.exe
setup/*.msi
setup/nginx-1.29.8/

# 자동 생성/로컬 산출물 (콘솔 U-push 의 git add -A 가 쓸어담지 않도록)
# (.gitignore 는 인라인 주석 미지원 → 패턴은 반드시 단독 줄로 둘 것)
# - ECM 자동화 디버그 스크린샷
playwright_error_*.png
# - Windows 바로가기(예: 서버 관리 콘솔 링크)
*.lnk

# Local test artifacts / generated output (not for VCS)
dummy/
output/
