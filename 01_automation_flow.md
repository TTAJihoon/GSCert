# 자동화 전체 흐름

## 목적

사용자가 웹페이지에서 프로젝트를 선택하면 서버가 해당 프로젝트들의 제출물을 순차적으로 다운로드하고, zip 내부 파일을 검사한 뒤 결과를 저장한다.

## 전체 단계

1. 프로젝트 목록 조회
2. 사용자 프로젝트 선택
3. 작업 생성 및 단일 작업 락 확인
4. 프로젝트별 순차 처리 시작
5. 웹페이지1에서 프로젝트 폴더 선택
6. 현재 페이지의 문서 목록 전체 선택
7. 파일 다운로드 메뉴 실행
8. Windows 폴더 선택 팝업 처리
9. 에이전트 전송현황 창 감지 및 완료 대기
10. zip 파일 확인
11. zip 내부 파일 검사
12. 프로젝트별/규칙별 결과 저장
13. 실패 이력 저장 후 다음 프로젝트 진행
14. 완료 결과 조회

## 확정된 정책

- 한 번에 하나의 작업만 실행한다.
- 작업 중 다른 사용자가 새 작업을 요청하면 진행 중 안내 메시지를 보여준다.
- 실패한 프로젝트는 실패 이력을 남기고 다음 프로젝트로 진행한다.
- 서버 또는 작업 재시작 시 완료된 프로젝트는 제외하고 재개한다.
- 로그인/권한 기능은 현재 적용하지 않는다.
- 모든 설계 및 폴더 설명 문서는 한글로 작성한다.
- 장기 다운로드/검사 작업은 Uvicorn 웹서버 내부가 아니라 별도 worker 프로세스에서 실행한다.
- 서버/worker는 개별 시작·중지 스크립트와 통합 시작·중지 스크립트를 모두 둔다.
- 각 폴더의 `readme.md`는 설계 정보가 아니라 해당 폴더의 하위 폴더와 파일 역할을 설명하는 가이드 문서로 작성한다.

## 아직 테스트로 검증할 항목

- Windows 폴더 선택 팝업에서 프로젝트별 폴더를 안정적으로 선택할 수 있는지
- 에이전트 전송현황 창이 사라지는 시점을 다운로드 완료로 보아도 되는지
- 중복 파일 시스템 알림이 뜬 뒤 폴더 변경 재시도가 안정적으로 가능한지
- worker를 숨김 창 백그라운드 프로세스로 실행해도 Windows GUI 팝업을 제어할 수 있는지

## 구현 순서

상세 구현 순서는 `12_implementation_roadmap.md`에서 관리한다.

초기 구현은 프로젝트 목록 조회, 작업 생성/락/상태 저장, worker dry-run, 운영 스크립트까지 먼저 진행한다.

실제 웹페이지1/에이전트 자동화는 그 다음 단계에서 테스트하며 붙인다.

## 남은 결정사항

아직 확정되지 않았거나 실제 테스트로 검증해야 하는 항목은 `15_open_decisions.md`에서 관리한다.
