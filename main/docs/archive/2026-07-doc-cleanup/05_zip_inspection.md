# zip 내부 검사 설계

## 목적

다운로드된 zip 파일 내부의 Word, Excel, PDF, 기타 파일들이 정해진 규칙대로 작성되었는지 검사한다.

## 실제 다운로드 형태

다운로드 시 zip 1개가 생성되는 것이 아니라 개별 파일들이 각각 다운로드된다.

따라서 '다운로드 확인' 단계(9단계)에서는 zip 손상 여부가 아니라 폴더 내 파일 존재/개수/크기를 확인한다.

검사 규칙(11단계)에서는 개별 파일(Word, Excel, PDF 등)을 직접 검사한다.

## 검사 결과 최소 단위

결과는 아래 단위까지 조회할 수 있어야 한다.

```text
작업
→ 프로젝트
→ zip 파일
→ 내부 파일
→ 검사 규칙
→ 검사 결과
```

## 규칙 저장 방식

규칙은 자주 바뀌지는 않지만 변경 가능성이 있으므로 DB에 저장하고, 상세 조건은 JSON으로 둔다.

예:

```json
{
  "sheet": "Sheet1",
  "cell": "B2",
  "expected_from": "ecm.프로젝트번호"
}
```

Word 표 검사 예:

```json
{
  "table_index": 0,
  "row": 2,
  "col": 1,
  "expected_from": "ecm.회사명"
}
```

파일명 검사 예:

```json
{
  "filename_contains": "ecm.프로젝트번호"
}
```

## 검사 유형 초안

- zip 파일명에 프로젝트번호 포함 여부
- zip 내부 파일명에 프로젝트번호 포함 여부
- Word 문서의 특정 표 위치 값 확인
- Excel 문서의 특정 셀 값 확인
- PDF 존재 여부 확인
- 특정 확장자 파일 존재 여부 확인
- 파일 저장시간 또는 수정시간 확인
- 필수 파일 누락 확인

## 실패 처리

검사 실패는 프로젝트 처리 실패와 구분한다.

- 다운로드 실패: 프로젝트 처리 실패
- zip 손상: 프로젝트 처리 실패
- 규칙 불일치: 검사 결과 실패
- 필수 파일 누락: 검사 결과 실패

## 추가로 정할 항목

- 규칙별 심각도
- 실패/경고/정보 상태 구분
- 검사 결과 화면에서 보여줄 메시지 형식
- 규칙 변경 시 버전 관리 방식

## 현재 권장 저장 방식

점검 결과는 프로젝트 최종 결과와 규칙별 상세 결과를 분리한다.

- 프로젝트 최종 결과: `DownloadReviewProject.review_status`
- 규칙별 상세 결과: `DownloadReviewRuleResult`
- 사용자 표시용 파일 정보: 규칙 결과의 `file_name`, `file_path`, `message`, `expected`, `actual`
- 관리자 확인용 원문 상세: `raw_detail_json` 또는 `DownloadReviewLog`
- 저장 범위: 작업에 포함되어 실제 점검한 프로젝트마다 규칙 개수만큼 저장한다.
- 저장 대상: 통과한 규칙과 실패한 규칙을 모두 저장한다.

판정 기준:

- 모든 규칙 통과: `review_status=completed`, `ecmlist.db` 점검결과 `O`
- 하나라도 부적합: `review_status=needs_fix`, `ecmlist.db` 점검결과 `X`
- `ecmlist.db`의 산출물별 점검 컬럼은 실제 점검규칙과 1:1로 대응하므로 해당 컬럼에 최신 `O/X`를 기록한다.
- 미실행/대상 없음 기본값은 빈 값으로 둔다.
- 다운로드/agent/분석 실행 실패: workflow DB에서는 실패/보류로 표시한다. `ecmlist.db` 점검결과와 산출물별 컬럼에는 규칙 판정 `O/X`만 기록한다.

사용자 화면에는 서버 절대경로를 표시하지 않는다. 프로젝트 폴더 기준 상대 경로 또는 파일명만 표시한다.

## 분석 완료 후 파일 정리

다운로드 파일과 폴더는 규칙 분석 완료 후 삭제한다.

삭제 전에 DB에 남겨야 하는 정보:

- 다운로드된 파일명
- 파일 크기
- 규칙별 판정 결과
- 사용자용 메시지
- 관리자 로그 event code

`ecmlist.db`는 삭제하지 않는다. 프로젝트별 점검결과와 산출물 컬럼만 추가/수정한다.

## 실제 규칙 기준

세부 규칙의 단일 원본은 `main/docs/19_inspection_rule_manual.md`다. 이 문서는 zip/다운로드 검사 설계 관점의 요약만 유지한다.

### 구현 상태

현재 `seed_download_review_rules --only-real --enable --update-existing` 기준 1~18번 실제 규칙이 모두 구현되어 있다.

| 번호 | 산출물 | 주요 검사 요약 |
| --- | --- | --- |
| 1 | 계약서 | 계약 폴더, PDF 1개, 파일명 `계약서`와 `{프로젝트번호}` 포함 |
| 2 | 합의서(PDF) | Word/PDF 1개씩, 시험신청번호, 머리말 프로젝트번호, 바닥글 양식번호, PDF 1페이지 캡처 |
| 3 | 수수료산정표 | 계약 폴더, `.xlsx` 1개, 파일명 `수수료산정표`와 `{프로젝트번호}` 포함 |
| 4 | 시험환경구성도 | 시험/계획 폴더, 파일명 `구성도`와 `{프로젝트번호}` 포함, `.png` 또는 `.pptx` 1개 이상 |
| 5 | 품질특성별제품정보기재사항 | Word 1개, 본문 `{프로젝트번호}`와 `품질특성별`, 다음 문단 날짜, 바닥글 서식번호 금지 |
| 6 | 기능리스트 | Excel 1개, 단일 시트, 제목/작성자, 머리글/바닥글 금지어, `대분류` 표 영역 캡처 |
| 7 | 시험계획서(PDF) | Word/PDF 1개씩, 일정/담당자/PL/제품정보/형상항목/WD/바닥글/세부사양 표 비교, PDF 캡처 |
| 8 | 최초/최종형상RawData | 설계 rawdata에서 이미지 5개 이상 후보 폴더 2개와 이미지 수정일자 시험기간 범위 확인 |
| 9 | 테스트케이스 | Excel 1개, 단일 시트, 제목/작성자/센터별 검토자/작성일, 잔여결함 F 개수, 머리글·바닥글 검사 |
| 10 | 결함리포트 | 수행 폴더, 버전별 Excel 파일 수·시트·시험환경·머리글·바닥글·보고일자 검사, `{잔여결함수}`, `{H}`, `{R}` 산출 |
| 11 | 점검표(PDF) | Excel/PDF, 표지/기능별 점검표/기능적합성/신뢰성/바닥글 검사, `{측정항목별점수표}` 산출, PDF 캡처 |
| 12 | 1차/2차/성능/보안RawData | 결함/보안/성능 rawdata 폴더 구조와 하위 항목 존재 검사 |
| 13 | 시험성적서(PDF) | Word/PDF 1개씩, 결함리포트 송부 일자와 `<세부사양>` 표 산출, 머리글/바닥글, PDF 캡처 |
| 14 | 시험기록서 | `기록서` PDF 1개 이상 존재 여부, PDF 1페이지 캡처 |
| 15 | 품질평가보고서 | Word 1개, 프로젝트번호 개수, 서명, 회사명, 신청/계약/시험/인증위 날짜, 품질검사표 산출값 비교 |
| 16 | 품질검사표 | Excel 1개, 시트명, 점검표 산출 점수표 비교, 품질부특성 측정값 산출, 바닥글 검사 |
| 17 | SW저작권확인서 | 인증관련 폴더, 파일명 `확인서`, PDF 1개 이상 |
| 18 | 홍보이미지 | 홍보 폴더, 파일 1개 이상, 파일명 `예시` 금지 |

### 공통 기준

- 검사 대상은 다운로드 폴더의 실제 파일과 테스트/이관용 zip 내부 파일 경로다.
- zip 내부 파일 경로는 `zip파일명::내부/경로/파일명` 형태로 결과에 저장한다.
- 파일명 조건은 단어 순서와 관계없이 필요한 단어가 모두 포함되는지만 검사한다.
- 파일명/키워드의 `-`와 `_` 차이는 무시한다. 예: `TTA_26_00492`는 `TTA-26-00492`와 동일하게 본다.
- 폴더 탐색, 파일명 키워드, 확장자별 개수, 문서 내용 검사 조건은 `inspection_rule.config_json`에 저장한다.
- 사용자에게는 서버 절대경로를 표시하지 않고 파일명 또는 프로젝트 폴더 기준 상대경로만 표시한다.

### 센터별 이름 기준

시험계획서 담당자, 테스트케이스 검토자, 점검표 표지 검토자는 센터별 expected 값을 사용한다.

| 센터 | expected |
| --- | --- |
| 분당(`bundang`) | `임우섭` |
| 상암(`sangam`) | `김진영` |
| 영남(`yeongnam`) | `이재훈` |
| 기본값 | `김진영` |

### 공유 엔진 버전

현재 rulebase manifest와 bundle은 `engine_min_version=0.2.0`을 내려준다. 로컬 앱은 `gscert_review_core.ENGINE_VERSION`과 비교해 낡은 앱/엔진이 신규 규칙셋을 실행하지 않도록 차단한다.

관련 API:

```text
GET /api/local-review/rules/manifest/
GET /api/local-review/rules/bundle/
```

## LLM 규칙 후보

파일 존재, 확장자, 파일명 포함 여부처럼 명확한 규칙은 프로그램 규칙으로 처리한다.

문서 본문을 읽고 의미를 판단해야 하는 규칙은 LLM 규칙 후보가 될 수 있다.
다만 현재는 실제 API 호출을 연결하지 않고, 아래 인터페이스만 먼저 구현했다.

- provider-neutral payload 생성
- Claude/GPT/Gemini/내부 GPU API로 변환 가능한 `messages` 구조
- 응답 JSON schema
- 응답 파서
- 수동 테스트용 `build_llm_review_prompt` 관리 명령

관련 문서:

- `17_llm_review_interface.md`

운영 연결 전까지 LLM 결과는 자동 write-back 대상이 아니다.
특히 `warning`은 `O/X`로 쓰지 않고 사용자 확인이 필요한 상태로 다루는 것이 안전하다.
