# 점검규칙 관리 매뉴얼

## 목적

이 문서는 download-review 점검규칙의 단일 기준 문서다. 1~18번 실제 규칙과 앞으로 추가될 모든 점검규칙은 이 문서에서 같은 형식으로 관리한다.

> **번호 기준**: 아래 번호(`##` 헤더, `규칙 목록`/`세부 점검 규칙 ID` 표)는 실제 "규칙별 점검 결과" 화면에 표시되는 순서, 즉 DB `sort_order` 실행 순서를 그대로 따른다(`main/views/review/ecm_download_review_inspection.py`가 `order_by("sort_order", "name", "id")`로 규칙을 읽고, `engine.evaluate_rules`가 그 순서 그대로 `enumerate`해 화면의 "번호" 열이 된다). DB `code`(`artifact_01`~`artifact_18`) 접미 번호는 최초 정의 당시 순서가 그대로 고정된 값이라 이 번호와 다르다(예: 시험성적서는 화면 번호 8이지만 `code`는 `artifact_13`). 각 규칙 항목의 `code:` 줄에서 실제 DB 코드를 확인할 수 있다.

과거 산출물 점검 설계 원문은 `main/docs/archive/2026-07-doc-cleanup/05_zip_inspection.md`에 보관했다. 최신 세부 규칙의 원본은 이 문서로 본다.

## 관련 코드와 문서

| 항목 | 위치 |
| --- | --- |
| 규칙 원본 문서 | `main/docs/03_inspection_rule_manual.md` |
| 과거 산출물 점검 설계 | `main/docs/archive/2026-07-doc-cleanup/05_zip_inspection.md` |
| DB 구조 | `main/docs/13_db_schema.md` |
| 규칙 seed 명령 | `main/management/commands/seed_download_review_rules.py` |
| 점검 엔진 | `main/views/review/ecm_download_review_inspection.py` |
| 결과 write-back | `main/views/review/ecm_reference_db.py` |
| Codex skill 요약 | `main/docs/codex_skills/gscert-download-review-maintainer/references/rules.md` |

## 규칙 상태

| 상태 | 의미 |
| --- | --- |
| `구현됨` | 현재 코드와 DB seed에 반영되어 실행 가능한 규칙 |
| `정의 확정` | 요구사항은 확정됐지만 아직 코드 미구현인 규칙 |
| `정의 중` | 추가 질문이나 샘플 확인이 필요한 규칙 |
| `연결 예정` | 다른 규칙 정의 또는 구현 이후 연결할 항목 |

## 공통 실행 흐름

1. worker가 프로젝트 산출물 zip 또는 다운로드 폴더를 확인한다.
2. `inspection_rule`에서 `enabled=True`인 규칙을 `sort_order` 순서로 읽는다.
3. 각 규칙은 `rule_type`에 맞는 검사 함수로 실행된다.
4. 검사 결과는 규칙별로 `inspection_result`에 저장한다.
5. 모든 규칙이 통과하면 프로젝트 점검결과는 `완료`, 하나라도 부적합이면 `수정 필요`가 된다.
6. 작업 자체가 실패하면 프로젝트 점검결과는 `보류`가 되며, 규칙별 `O/X` write-back은 수행하지 않는다.
7. `config_json.artifact_column`이 `ecm_list` 산출물 컬럼과 매핑되면 해당 컬럼에 `O` 또는 `X`를 write-back한다.
8. 규칙이 `raw_detail_json.variables`에 저장한 값은 같은 실행의 후속 규칙에서 `{변수명}` 자리표시자로 참조할 수 있다.
9. `config_json.disabled_sub_checks`(1-based 세부항목 순번 문자열 목록)에 들어있는 세부항목은 `engine._apply_disabled_sub_checks`가 결과에서 완전히 제외하고 status/expected/actual을 남은 항목만으로 다시 계산한다. 규칙 전체 on/off는 `enabled` 필드, 세부항목 on/off는 이 키를 쓴다. 서버 관리 콘솔(`launcher.ps1`) `D` 메뉴 → `2) 수정`에서 `manage.py rule_toggle`로 조작하며, 세부항목 라벨은 `main/rule_sub_check_catalog.py`(이 문서의 "세부 점검 규칙 ID" 표를 옮긴 참고용 카탈로그) 기준으로 표시된다.

## 공통 변수

| 변수 | 값 | 원천 |
| --- | --- | --- |
| `{project_number}` | 프로젝트 번호 | `DownloadReviewProject.project_number` |
| `{프로젝트번호}` | 프로젝트 번호 | `DownloadReviewProject.project_number` |
| `{product}` | 버전 값을 제거한 제품명 | `ecm_row_json["제품명"]` 또는 `ecm_row_json["product"]` |
| `{제품명}` | 버전 값을 제거한 제품명 | `ecm_row_json["제품명"]` 또는 `ecm_row_json["product"]` |
| `{company}` | 회사명 | `ecm_row_json["회사명"]` 또는 `ecm_row_json["company"]` |
| `{회사명}` | 회사명 | `ecm_row_json["회사명"]` 또는 `ecm_row_json["company"]` |
| `{버전}` | 제품명 끝부분에서 추출한 버전 | 제품명 파생값 |
| `{pl}` | 시험PL | `ecm_row_json["시험PL"]` 또는 `ecm_row_json["pl"]` |
| `{PL}` | 시험PL | `ecm_row_json["시험PL"]` 또는 `ecm_row_json["pl"]` |
| `{wd}` | WD | `ecm_row_json["WD"]` 또는 `ecm_row_json["wd"]` |
| `{WD}` | WD | `ecm_row_json["WD"]` 또는 `ecm_row_json["wd"]` |
| `{시작일}` | 시험 시작일 | `reference_project.start_date` 또는 API 응답 `project.start_date` |
| `{종료일}` | 시험 종료일 | `reference_project.expected_end_date` 또는 API 응답 `project.end_date` |
| `{연도}` | 프로젝트 연도 | `{프로젝트번호}`의 `TTA-YY-xxxxx`에서 `20YY`로 추출 |
| `{잔여결함수}` | 잔여 결함 개수 | 10번 결함리포트 규칙에서 산출 |
| `{결함차수}` | 결함 차수 | 8번 시험성적서 규칙에서 산출 |
| `{1차}`, `{2차}` 등 | 차수별 결함리포트 보고일자 | 8번 시험성적서 규칙에서 산출 |
| `{H}` | High 결함 개수 | 10번 결함리포트 규칙에서 산출 |
| `{R}` | 수정전 결함 개수 | 10번 결함리포트 규칙에서 산출 |
| `{측정항목별점수표}` | 점검표의 측정항목별 점수표 값 목록 | 12번 점검표 규칙에서 산출 |
| `{품질부특성측정값}` | 품질검사표의 품질부특성 측정값 목록 | 15번 품질검사표 규칙에서 산출 |
| `{신청일}` | 신청일 | `reference_project.request_date` |
| `{계약일}` | 계약일 | `reference_project.contract_date` |
| `{인증위}` | 품질인증심의위원회 일자 | `reference_project.cert_date` 또는 `cert_committee_date` |

### 변수 세부 규칙

- `{버전}`은 제품명 끝부분에서 `v`, `V`, `ver`, `VER`, `version` 등으로 시작하는 버전 값을 우선 추출한다.
- `v1`, `v1.0`, `ver 3.0`, `version Enterprise`처럼 접두사 뒤 값이 숫자 또는 문자여도 버전으로 본다.
- 버전 접두사가 없으면 제품명의 마지막 공백 이후 마지막 단어를 `{버전}`으로 본다. 예: `자료분석 플랫폼 Enterprise`는 제품명 `자료분석 플랫폼`, 버전 `Enterprise`이다.
- 마지막 단어에 문자와 숫자가 붙어 있으면 숫자 버전만 떼어 `{버전}`으로 보고, 앞쪽 문자는 `{제품명}`에 남긴다. 예: `EBS ISM3.0`은 제품명 `EBS ISM`, 버전 `3.0`이다.
- 제품명에 공백이 없고 접두사 또는 숫자 버전도 없으면 `{버전}`은 찾지 못한 것으로 처리한다.
- `{제품명}`과 `{product}`는 원래 제품명에서 `{버전}`으로 추출된 부분을 제거한 값이다.
- 인증획득목록 제품명이 `국문명 버전(영문명 버전)` 형식으로 국문명/영문명을 병기한 경우, 국문명은 `{제품명}`/`{product}`로, 영문명은 별도로 파싱해 9-5(제품/버전/신청번호) 검사에서 국문명 또는 영문명 둘 중 하나만 적혀 있어도 일치로 인정한다. 병기 형식이 아니면(괄호 없이 단일 명칭만 있는 경우) 영문명 후보는 비어 있고 국문명만 비교한다.
- 병기 형식은 `국문명 버전(영문명 버전)`처럼 한 줄에 적힐 때도 있고, `국문명 버전\n(영문명 버전)`처럼 국문명과 영문명이 줄바꿈으로 나뉘어 적힐 때도 있다. `{제품명}`은 원본 값의 줄바꿈을 공백으로 합친 뒤 위 규칙으로 파싱하므로 두 형식 모두 영문명이 인식된다.
- `{회사명}`/`{company}` 원본 값이 `국문명\n영문명`처럼 줄바꿈으로 병기된 경우(예: `㈜이든티앤에스\nEDEN TNS Inc`), 첫 줄은 `{회사명}`/`{company}`로, 둘째 줄은 영문명 후보로 각각 분리한다. 16-4(회사명) 검사는 품질평가보고서의 회사(기관)명 셀이 국문명 또는 영문명 둘 중 하나와만 일치해도 적합으로 인정한다(제품명과 동일한 이유).
- `{시작일}`과 `{종료일}`은 기준정보 API 또는 `reference_project`에서 기준 행을 찾지 못하거나 값이 비어 있으면 기준정보 없음으로 실패 처리한다.
- `{시작일}` 날짜 형식은 `yyyy.mm.dd.`를 기준으로 한다.
- `{잔여결함수}`는 10번 결함리포트의 마지막 버전 파일 내 `최종결함리포트` 시트에서 산출한다.
- `{1차}`, `{2차}` 등 차수별 보고일자는 8번 시험성적서의 `결함리포트 송부` 표에서 산출한다.
- `{결함차수}`는 8번 시험성적서에서 산출된 차수별 보고일자 개수로 본다.
- 후속 규칙 변수는 규칙 실행 순서 기준으로 앞선 규칙 결과에서만 참조한다. 8번 시험성적서는 10번 결함리포트보다 먼저 실행되도록 seed `sort_order=95`로 고정한다.
- `{H}`와 `{R}`은 10번 결함리포트의 마지막 버전 파일 내 `시험분석자료` 시트에서 산출한다.
- `{측정항목별점수표}`는 12번 점검표의 `측정항목별 점수표` 시트 D7~D90 값에서 산출한다.
- `{품질부특성측정값}`은 15번 품질검사표의 E4:E85 실제 값 33개 중 27번째 값을 제외하고 `4~26, 28~33, 1~3` 순서로 재정렬해 산출한다.
- `{신청일}`과 `{계약일}`은 Google Sheet 적재 결과인 `reference_project.request_date`, `reference_project.contract_date` 값을 사용한다.
- `{인증위}`는 `reference_project.cert_date` 또는 정규화된 인증위 날짜 값을 사용한다.

## 센터별 이름 기준

시험계획서 담당자, 테스트케이스 검토자, 점검표 표지 검토자는 센터 코드에 따라 다른 기대값을 사용한다.

| 센터 코드 | 센터 | 기대 이름 |
| --- | --- | --- |
| `bundang` | 분당 | `임우섭` |
| `sangam` | 상암 | `김진영` |
| `yeongnam` | 영남 | `이재훈` |
| 기타/미지정 | 기본값 | `김진영` |

구현은 `config_json`의 `manager_expected_by_center`, `reviewer_expected_by_center`, `cover_author_by_center`를 읽고, 센터 매핑이 없으면 각각의 기본 expected 값을 사용한다.

## 공통 폴더 탐색 방식

`folder_keyword_chain`은 폴더 경로에서 키워드를 순서대로 찾는다.

예를 들어 `["시험", "계획"]`이면 먼저 `시험`이 포함된 폴더를 찾고, 그 하위에서 `계획`이 포함된 폴더를 찾는다. 조건을 만족하는 폴더가 선택되면 해당 폴더 아래 파일을 검사한다.

같은 조건을 만족하는 폴더가 여러 개일 수 있으면 실제 샘플로 확인하고, 필요한 경우 추가 키워드 또는 선택 기준을 별도로 정의한다.

`config_json.folder_fallback_search_all: true`를 설정하면, 폴더 위치는 참고 정보일 뿐 적합/부적합을 가르는 필수 조건이 아니게 된다. `folder_keyword_chain`으로 폴더를 찾을 수 있으면 화면에는 그 폴더를 표시하지만, 실제로 파일을 찾는 범위는 항상 폴더 제한 없이 전체 제출물이다 — 즉 파일이 어디에 있든 파일명 키워드와 확장자가 맞으면 매칭 대상이 되고, 내용 검사도 동일하게 수행된다. 파일명 키워드 검사는 각 평가 함수에서 그대로 수행되므로 무관한 파일을 잘못 통과시키지는 않는다.
  - 이 완화는 두 가지 실제 사례를 모두 해결한다: (1) 개정판(v1.1 등)이 원래 폴더 밖에 별도로 올라오는 경우, (2) 원본(v1.0)조차 폴더 구조를 지키지 않고 단독으로 올라온 경우(예: 계약서가 `2.계약` 폴더가 아니라 수정내용.txt와 함께 제출물 루트에 올라온 경우).
  - 같은 이름 조건에 맞는 파일이 올바른 폴더와 다른 위치에 동시에 존재하면(중복 제출), `_latest_revision_files`가 다음 기준으로 하나만 남긴다: 파일명에 버전이 있으면 더 높은 버전을, 버전이 동일하거나 파일명에 버전이 전혀 없으면 파일 수정 날짜(`modified_at`)가 더 최신인 파일을 선택한다. 그래서 `exact_count` 조건이 중복 때문에 "2개 이상"으로 잘못 부적합 처리되지 않는다.

`folder_keyword_chain`을 쓰는 규칙 중 rawdata 관련 2개(1차/2차/성능/보안RawData, 최초/최종형상RawData — 각각 `folder_checks`/자체 폴백 로직을 써서 이 플래그가 적용되지 않음)와 시험기록서(애초에 폴더 제한이 `[]`라 항상 전체 검색)를 제외한 **나머지 15개 규칙 전부**에 `folder_fallback_search_all: true`가 적용되어 있다(계약서, 합의서, 수수료산정표, 시험환경구성도, 품질특성별제품정보기재사항, 기능리스트, 시험계획서, 테스트케이스, 결함리포트, 점검표, 시험성적서, 품질평가보고서, 품질검사표, SW저작권확인서, 홍보이미지).

## 공통 파일 매칭 방식

- `filename_keywords`의 모든 키워드가 파일명에 포함되어야 한다.
- 키워드 순서는 상관없다.
- `extensions`가 비어 있으면 확장자를 제한하지 않는다.
- `exact_count`가 있으면 정확한 파일 개수를 요구한다.
- `min_count`는 최소 파일 개수를 요구하며, `exact_count`가 있으면 `exact_count`가 우선이다.
- 사용자가 보는 경로는 서버 절대경로가 아니라 프로젝트 번호가 포함된 하위 경로를 우선 표시한다.
- 파일명 끝에 산출물 개정 버전 토큰이 있으면 같은 산출물 후보를 정리한 뒤 검사한다.
  - 지원 예: `v1`, `v1.0`, `v1.1`, `ver 1.1`, `version 1.1`, `1.1`.
  - 같은 산출물 identity에서는 versioned 파일이 unversioned 파일보다 우선한다.
  - 같은 major 안에서는 최신 minor만 유지한다. 같은 major/minor가 여러 개이면 파일 수정일이 가장 최신인 파일을 유지한다.
  - 기존 산출물 폴더에 과거 버전이 있고 별도 위치에 새 버전이 추가된 경우, 같은 파일명 그룹이면 새 버전 파일을 검사 대상으로 끌어온다.

## 공통 문서 파싱 주의

- `.docx` 본문 텍스트는 문단 단위로 run(`w:t`)을 공백 없이 이어붙인 뒤 문단끼리 공백으로 합친다. Word가 한 숫자/단어를 여러 run으로 쪼개기 때문에(예: `2026`이 `20`,`2`,`6`으로 분리), run을 공백으로 합치면 날짜·프로젝트번호 검사가 깨진다.
- `.docx` 전체 텍스트 검사(예: 프로젝트번호 등장 횟수)는 본문(`document.xml`)뿐 아니라 머리말(`header*.xml`), 바닥글(`footer*.xml`)도 포함한다.
- Excel 셀 값이 정수 `0`일 때 빈 문자열로 읽지 않는다. 결함 개수·점수가 `0`인 셀(예: 신뢰성 `{R}`)도 `"0"`으로 정확히 비교한다.

## 저장 산출물

| 산출물 | 저장/표시 정책 |
| --- | --- |
| PDF 1페이지 캡처 | 규칙 결과에서 버튼으로 조회할 수 있게 저장한다. |
| Excel 영역 이미지 | 규칙 결과에서 버튼으로 조회할 수 있게 저장한다. |
| 사용자용 오류 메시지 | `inspection_result.message`, `expected`, `actual`에 저장한다. |
| 상세 증거 | `inspection_result.raw_detail_json`에 저장한다. |
| 내부 stack trace | 사용자에게 노출하지 않고 관리자 로그에 저장한다. |

이미지 파일은 `workflow.db`에 직접 넣지 않고 `main/data/download_review_artifacts/` 아래에 저장하고, DB에는 조회용 메타데이터만 저장한다.

조회 API:

```text
GET /api/rule-results/{result_id}/artifacts/{artifact_id}/
```

API는 `inspection_result.raw_detail_json.artifacts`에 저장된 상대 경로만 사용하며 서버 절대경로는 응답에 노출하지 않는다.

## 규칙 목록

| 번호 | 규칙명 | 상태 | write-back 컬럼 | code |
| --- | --- | --- | --- | --- |
| 1 | 계약서 | 구현됨 | `계약서` | `artifact_01` |
| 2 | 합의서 | 구현됨 | `합의서(PDF)` | `artifact_02` |
| 3 | 수수료산정표 | 구현됨 | `수수료산정표` | `artifact_03` |
| 4 | 시험환경구성도 | 구현됨 | `시험환경구성도` | `artifact_04` |
| 5 | 품질특성별제품정보기재사항 | 구현됨 | `품질특성별제품정보기재사항` | `artifact_05` |
| 6 | 기능리스트 | 구현됨 | `기능리스트` | `artifact_06` |
| 7 | 제품 스크린샷 | 구현됨 | `최초/최종형상RawData` | `artifact_08` |
| 8 | 시험성적서 | 구현됨 | `시험성적서(PDF)` | `artifact_13` |
| 9 | 시험계획서 | 구현됨 | `시험계획서(PDF)` | `artifact_07` |
| 10 | 결함리포트 | 구현됨 | `결함리포트` | `artifact_10` |
| 11 | 테스트케이스 | 구현됨 | `테스트케이스` | `artifact_09` |
| 12 | 점검표 | 구현됨 | `점검표(PDF)` | `artifact_11` |
| 13 | 1차/2차/성능/보안RawData | 구현됨 | `1차/2차/성능/보안RawData` | `artifact_12` |
| 14 | 시험기록서 | 구현됨 | `시험기록서` | `artifact_14` |
| 15 | 품질검사표 | 구현됨 | `품질검사표` | `artifact_16` |
| 16 | 품질평가보고서 | 구현됨 | `품질평가보고서` | `artifact_15` |
| 17 | SW저작권확인서 | 구현됨 | `SW저작권확인서` | `artifact_17` |
| 18 | 홍보이미지 | 구현됨 | `홍보이미지` | `artifact_18` |

## 세부 점검 규칙 ID

아래 ID는 문서와 상세 점검 결과를 대조하기 위한 관리용 식별자다. 실제 DB 규칙 코드는 `artifact_01`~`artifact_18`이고, 엔진이 `raw_detail_json.sub_checks` 또는 `checks`를 생성하는 규칙은 화면에서 하위검사 행으로 나뉘어 표시된다.

| ID | 점검 내용 | 판정 기준 | 표시/산출 증거 |
| --- | --- | --- | --- |
| 1-1 | 계약 폴더 탐색 | 경로에 `계약`이 포함된 폴더를 찾는다. | 선택된 폴더 또는 폴더 없음 메시지 |
| 1-2 | 계약서 파일명 | 파일명에 `계약서`와 `{project_number}`가 모두 포함되어야 한다. | 매칭 파일명 목록 |
| 1-3 | 계약서 확장자 | 매칭 파일은 `.pdf`여야 한다. | 확장자 불일치 파일 목록 |
| 1-4 | 계약서 개수 | 조건을 만족하는 PDF가 정확히 1개여야 한다. | 실제 매칭 개수 |
| 2-1 | 합의서 폴더 탐색 | `계약` 폴더 아래에서 검사한다. | 선택된 폴더 |
| 2-2 | 합의서 파일 세트 | `{project_number}`와 `합의서`를 포함한 Word 1개와 PDF 1개가 있어야 한다. | Word/PDF 매칭 파일명 |
| 2-3 | 시험신청번호 표 값 | Word 표에서 `시험신청번호` 오른쪽 셀이 `{project_number}`와 같아야 한다. | 추출된 셀 값 |
| 2-4 | 합의서 머리글 | Word 머리글에 `{project_number}`가 포함되어야 한다. | 머리글 텍스트 |
| 2-5 | 합의서 바닥글 | Word 바닥글에 `TIS-0101-3 (00)`이 포함되어야 한다. | 바닥글 텍스트 |
| 2-6 | 합의서 PDF 산출물 | 합의서 PDF 1페이지 캡처를 저장해 조회 가능해야 한다. | PDF 1페이지 이미지 artifact |
| 3-1 | 수수료산정표 폴더 탐색 | `계약` 폴더 아래에서 검사한다. | 선택된 폴더 |
| 3-2 | 수수료산정표 파일명 | 파일명에 `수수료산정표`와 `{project_number}`가 모두 포함되어야 한다. | 매칭 파일명 목록 |
| 3-3 | 수수료산정표 파일 형식/개수 | `.xlsx` 파일이 정확히 1개여야 한다. | 실제 확장자와 개수 |
| 4-1 | 시험환경구성도 폴더 탐색 | `시험` 폴더 하위의 `계획` 폴더에서 검사한다. | 선택된 폴더 |
| 4-2 | 시험환경구성도 파일명 | 파일명에 `구성도`와 `{project_number}`가 모두 포함되어야 한다. | 매칭 파일명 목록 |
| 4-3 | 시험환경구성도 형식 | `.png` 또는 `.pptx` 파일이어야 한다. | 확장자별 매칭 목록 |
| 4-4 | 시험환경구성도 개수 | 조건을 만족하는 파일이 1개 이상이어야 한다. | 실제 매칭 개수 |
| 5-1 | 품질특성별제품정보기재사항 폴더 탐색 | `시험` 폴더 하위의 `계획` 폴더에서 검사한다. | 선택된 폴더 |
| 5-2 | 품질특성별제품정보기재사항 파일 세트 | `{project_number}`와 문서명을 포함한 Word 파일이 정확히 1개 있어야 한다. | 매칭 파일명 |
| 5-3 | 제목 문구 | 첫 페이지 제목 영역에 `{project_number}`와 `품질특성별` 문구가 있어야 한다. | 제목 추출 텍스트 |
| 5-4 | 제목 다음 문단 날짜 | 제목 다음 문단이 날짜 형식이어야 한다. | 추출 날짜 문자열 |
| 5-5 | 바닥글 금지어 | 바닥글에 `TPG`, `TIS`가 없어야 한다. | 바닥글 텍스트와 금지어 매칭 |
| 6-1 | 기능리스트 파일 세트 | `시험` > `계획` 폴더에 `{project_number}`와 `기능리스트`를 포함한 Excel 1개가 있어야 한다. | 매칭 파일명 |
| 6-2 | 기능리스트 시트 수 | 통합문서의 시트가 정확히 1개여야 한다. | 시트명 목록 |
| 6-3 | 기능리스트 제목 | 제목 셀에 `{project_number} 기능리스트`가 포함되어야 한다. | 제목 추출값 |
| 6-4 | 기능리스트 작성자 | 작성자 라벨의 값이 `{pl}`과 같아야 한다. | 작성자 추출값 |
| 6-5 | 기능 표 기준 행 | `대분류` 앵커를 찾고 하위 기능 데이터가 있어야 한다. | 앵커 위치와 추출 행 수 |
| 6-6 | 기능리스트 머리글 금지어 | 인쇄 머리글에 `TTA`가 없어야 한다. | 머리글 텍스트 |
| 6-7 | 기능리스트 바닥글 금지어 | 인쇄 바닥글에 `TPG`, `TIS`가 없어야 한다. | 바닥글 텍스트 |
| 7-1 | 제품 스크린샷 폴더 탐색 | `설계` 폴더 또는 스크린샷/형상 후보 폴더를 찾는다. | 후보 폴더 목록 |
| 7-2 | 최초/최종 폴더 수 | 이미지 5개 이상을 가진 후보 폴더가 2개 이상이어야 한다. | 후보 폴더별 이미지 수 |
| 7-3 | 이미지 파일 수 | 각 후보 폴더에는 이미지 파일이 5개 이상 있어야 한다. | 이미지 파일 목록 |
| 7-4 | 이미지 수정일 범위 | 각 이미지 수정일이 `{시작일}`부터 `{종료일}` 사이여야 한다. | 범위 밖 파일과 수정일 |
| 7-5 | 스크린샷 실제값 표시 | 범위 밖 파일이 없으면 정상, 있으면 파일 수와 날짜를 표시한다. | raw detail 파일 목록 |
| 8-1 | 시험성적서 파일 세트 | `시험` > `종료` 폴더에 Word 1개와 PDF 1개가 있어야 한다. | Word/PDF 매칭 파일명 |
| 8-2 | 결함리포트 송부 표 | Word 문서에서 차수별 결함리포트 송부 일자를 추출한다. | `{1차}`, `{2차}` 등 산출값 |
| 8-3 | 결함차수 산출 | 송부 일자 개수를 `{결함차수}`로 저장한다. | 결함차수 값 |
| 8-4 | 세부사양 표 산출 | `<세부사양>` 표를 추출해 시험계획서 비교용 변수로 저장한다. | `시험성적서_세부사양표` |
| 8-5 | 시험성적서 머리글 | 머리글에 `{project_number}`가 포함되어야 한다. | 머리글 텍스트 |
| 8-6 | 시험성적서 바닥글 | 바닥글에 `TPG-1016-5(02)`가 포함되어야 한다. | 바닥글 텍스트 |
| 8-7 | 시험성적서 PDF 산출물 | PDF 1페이지 캡처를 저장해 조회 가능해야 한다. | PDF 1페이지 이미지 artifact |
| 9-1 | 시험계획서 파일 세트 | `시험` > `계획` 폴더에 Word 1개와 PDF 1개가 있어야 한다. | Word/PDF 매칭 파일명 |
| 9-2 | 시험 시작일 | 첫 번째 표의 시험 시작일이 `{시작일}`과 같아야 한다. | 표 셀 날짜 |
| 9-3 | 시험계획서 담당자 | 담당자는 센터별 기준값과 일치해야 한다. | 담당자 셀 값 |
| 9-4 | 시험계획서 PL | 시험 PL 값이 `{pl}`과 일치해야 한다. | PL 셀 값 |
| 9-5 | 제품/버전/신청번호 | 제품명, 버전, 시험신청번호가 기준정보와 일치해야 한다. | 각 라벨 우측 셀 값 |
| 9-6 | 형상항목 ID | 형상항목 또는 관련 표에 `{project_number}`가 포함되어야 한다. | 형상항목 추출값 |
| 9-7 | 시험일정 WD | 시험일정 표의 WD 값이 `{wd}`와 일치해야 한다. | WD 셀 값 |
| 9-8 | 바닥글 Copyright | 바닥글에 `Copyright {연도} TTA`가 포함되어야 한다. | 바닥글 텍스트 |
| 9-9 | 시험계획서 바닥글 금지어 | 바닥글에 `TIS-`, `TPG`, `TIS`, `소프트웨어시험인증연구소`가 없어야 한다. | 금지어 매칭 결과 |
| 9-10 | 세부사양 표 비교 | 시험성적서에서 산출한 세부사양 표와 시험계획서 세부사양 표가 일치해야 한다. | 표 비교 결과 |
| 9-11 | 시험계획서 PDF 산출물 | PDF 1페이지 캡처를 저장해 조회 가능해야 한다. | PDF 1페이지 이미지 artifact |
| 10-1 | 결함리포트 파일 세트 | `수행` 폴더에 `{project_number}`와 `결함리포트`를 포함한 Excel 파일들이 있어야 한다. | 매칭 파일명 목록 |
| 10-2 | 결함차수 참조 | 8번 시험성적서가 산출한 `{결함차수}`를 참조할 수 있어야 한다. | 참조 변수 값 |
| 10-3 | 결함리포트 파일 수 | 파일 수는 `{결함차수}+1`이어야 한다. | 실제 파일 수 |
| 10-4 | 버전 연속성 | 파일 버전은 major 차수 기준으로 누락 없이 이어져야 하며, 같은 major 안에서는 최신 minor만 유지한다. | 버전 파싱 결과 |
| 10-5 | 결함리포트 시트 구성 | 각 파일에 차수별 필수 시트와 최종/분석 시트가 있어야 한다. | 시트명 비교 결과 |
| 10-6 | 시험환경 일관성 | 각 관련 시트의 시험환경 값이 서로 같아야 한다. | 시험환경 추출값 |
| 10-7 | 결함리포트 머리글 금지어 | 인쇄 머리글에 프로젝트번호 등 금지 텍스트가 없어야 한다. | 머리글 텍스트 |
| 10-8 | 결함리포트 바닥글 금지어 | 바닥글에 `TPG`, `TIS`, `소프트웨어시험인증연구소`가 없어야 한다. | 바닥글 텍스트 |
| 10-9 | 보고일자 일치 | 각 차수 보고일자가 `{1차}`, `{2차}` 등 기준값과 일치해야 한다. | 차수별 보고일자 |
| 10-10 | 결함 변수 산출 | 최종 파일에서 `{잔여결함수}`, `{H}`, `{R}`을 산출한다. | 산출 변수 값 |
| 10-11 | 품질특성별 결함내역 일치 | `시험분석자료`에서 G열의 `품질특성` 헤더 셀 바로 다음 행부터 연속값(결함 목록)을 모아, 품질특성 키워드별 개수가 `수정전 결함수` 요약표(첫 번째 `수정전` 라벨 아래 1~9행)와 일치해야 한다. | 품질특성별 기대/실제 개수 |
| 10-12 | 결함정도별 결함내역 일치 | `시험분석자료`에서 E열의 `결함정도` 헤더 셀 바로 다음 행부터 연속값(결함 목록)을 모아, H/M/L 개수가 `수정전 결함수` 요약표(두 번째 `수정전` 라벨 아래 1~3행)와 일치해야 한다. | H/M/L 기대/실제 개수 |
| 11-1 | 테스트케이스 파일 세트 | `설계` 폴더에 `{project_number}`와 `테스트케이스`를 포함한 Excel 1개가 있어야 한다. | 매칭 파일명 |
| 11-2 | 테스트케이스 시트 수 | 통합문서의 시트가 정확히 1개여야 한다. | 시트명 목록 |
| 11-3 | 테스트케이스 제목 | 제목 영역에 `{project_number}`와 `테스트케이스`가 포함되어야 한다. | 제목 추출값 |
| 11-4 | 작성자/검토자 | 작성자는 `{pl}`, 검토자는 센터별 기준값과 일치해야 한다. | 작성자/검토자 추출값 |
| 11-5 | 작성기간 | 작성기간이 `{시작일}`부터 `{종료일}` 범위와 일치해야 한다. | 작성기간 추출값 |
| 11-6 | 잔여결함수 | 상세 테스트 결과의 `F` 개수가 `{잔여결함수}`와 같아야 한다. | `F` 카운트 |
| 11-7 | 테스트케이스 머리글 금지어 | 인쇄 머리글에 `TTA`가 없어야 한다. | 머리글 텍스트 |
| 11-8 | 테스트케이스 바닥글 기준 | 바닥글에는 `TTA`가 있어야 하며 `TPG`, `TIS`, `소프트웨어시험인증연구소`는 없어야 한다. | 바닥글 텍스트 |
| 12-1 | 점검표 파일 세트 | `설계` 폴더에 점검표 Excel 1개와 PDF 1개가 있어야 한다. | Excel/PDF 매칭 파일명 |
| 12-2 | 점검표 머리글 | 머리글에 `{project_number}`가 포함되어야 한다. | 머리글 텍스트 |
| 12-3 | 점검표 바닥글 기준 | 바닥글에는 `한국정보통신기술협회`가 있어야 하며 `TIS-`, `TPG`, `TIS`, `소프트웨어시험인증연구소`는 없어야 한다. | 바닥글 텍스트 |
| 12-4 | 표지 제목/기간 | 표지 제목과 시험기간이 기준정보와 일치해야 한다. | 표지 추출값 |
| 12-5 | 표지 작성자/검토자 | 작성자는 `{pl}`, 검토자는 센터별 기준값과 일치해야 한다. | 작성자/검토자 추출값 |
| 12-6 | 기능별 점검표 빈셀 | 기능별 점검표 필수 범위에 빈 셀이 없어야 한다. | 빈 셀 위치 |
| 12-7 | 기능적합성 비교 | 기능적합성 표의 기대값과 결과값이 일치해야 한다. | 비교 결과 |
| 12-8 | 기능적합성 결과 | 기능적합성 결과 값이 비어 있지 않아야 한다. | 결과 셀 값 |
| 12-9 | 신뢰성 값 | 신뢰성 표에서 C5=`{wd}`, C11=`{H}`, E11=`{R}`이어야 한다. | 신뢰성 셀 값 |
| 12-10 | 측정항목별 점수표 산출 | `측정항목별 점수표` 시트 D7:D90 값을 `{측정항목별점수표}`로 저장한다. | 산출 변수 목록 |
| 12-11 | 점검표 PDF 산출물 | PDF 1페이지 캡처를 저장해 조회 가능해야 한다. | PDF 1페이지 이미지 artifact |
| 13-1 | RawData 전체 범위 | zip 전체에서 결함/보안/성능 RawData 구조를 검사한다. | 검사 루트 |
| 13-2 | 결함 RawData | `결함` 폴더에 이미지 파일이 1개 이상 있어야 한다. | 결함 폴더 파일 목록 |
| 13-3 | 보안 RawData 하위 폴더 | `보안` 폴더에는 하위 폴더가 2개 이상 있어야 한다. | 보안 하위 폴더 목록 |
| 13-4 | 보안 RawData 파일 | 각 보안 하위 폴더에는 파일 또는 폴더가 1개 이상 있어야 한다. | 하위 폴더별 항목 수 |
| 13-5 | 보안성 파일명 예외 | 파일명에 `보안성`, `인빅티`, `invicti`, `수행`, `대상`, `시험`, `면제` 중 하나라도 포함되면 보안 자료로 인정할 수 있다(OR 매칭). | 파일명 매칭 결과 |
| 13-6 | 성능 RawData | `성능` 폴더에 항목이 1개 이상 있어야 한다. | 성능 폴더 항목 수 |
| 14-1 | 시험기록서 검색 범위 | 특정 폴더에 제한하지 않고 전체 대상에서 검색한다. | 검색 루트 |
| 14-2 | 시험기록서 파일명 | 파일명에 `기록서`가 포함되어야 한다. | 매칭 파일명 목록 |
| 14-3 | 시험기록서 형식/개수 | `.pdf` 파일이 1개 이상 있어야 한다. | 실제 PDF 개수 |
| 14-4 | 시험기록서 PDF 산출물 | PDF 1페이지 캡처를 저장해 조회 가능해야 한다. | PDF 1페이지 이미지 artifact |
| 15-1 | 품질검사표 파일 세트 | `인증관련` 폴더에 `{project_number}`와 `품질검사표`를 포함한 Excel 1개가 있어야 한다. | 매칭 파일명 |
| 15-2 | 품질검사표 시트명 | 시트명은 `{project_number} 품질검사표`여야 한다. | 시트명 목록 |
| 15-3 | 품질부특성 측정값 개수 | E4:E85 범위에서 27번째 값을 제외한 33개 값을 산출해야 한다. | 측정값 개수 |
| 15-4 | 점수표 비교 | 품질검사표 점수표가 `{측정항목별점수표}`와 일치해야 한다. | 점수표 비교 결과 |
| 15-5 | 품질검사표 바닥글 기준 | 바닥글에는 `한국정보통신기술협회`가 있어야 하며 `TPG`, `TIS`, `소프트웨어시험인증연구소`는 없어야 한다. | 바닥글 텍스트 |
| 15-6 | 품질부특성 측정값 산출 | 산출한 값을 `4~26, 28~33, 1~3` 순서로 재정렬해 `{품질부특성측정값}`으로 저장한다. | 산출 변수 목록 |
| 16-1 | 품질평가보고서 파일 세트 | `인증관련` 폴더에 `{project_number}`와 `품질평가보고서`를 포함한 Word 1개가 있어야 한다. | 매칭 파일명 |
| 16-2 | 프로젝트번호 등장 횟수 | 문서 전체에서 `{project_number}`가 정확히 6회 등장해야 한다. | 등장 횟수 |
| 16-3 | 서명자 | 김성희, 정성룡 서명 문구가 기준 형식으로 있어야 한다. | 서명 영역 텍스트 |
| 16-4 | 회사명 | 회사명 항목이 `{company}`(국문명) 또는 그 영문명 병기값(있는 경우) 중 하나와 일치해야 한다. | 회사명 추출값 |
| 16-5 | 신청일/계약일 | 신청일과 계약일이 기준 DB 값과 일치해야 한다. | 날짜 추출값 |
| 16-6 | 시험기간/인증위 날짜 | 시험기간과 인증위 날짜가 기준값과 일치해야 한다. | 기간/날짜 추출값 |
| 16-7 | 품질부특성 표 | 마지막 품질부특성 평가 결과 표를 찾을 수 있어야 한다. | 표 위치 |
| 16-8 | 품질부특성 측정값 비교 | 표의 측정값이 `{품질부특성측정값}`과 일치해야 한다. | 비교 결과 |
| 16-9 | N/A 문구 | 측정값이 `NA`인 항목은 옆 셀에 `해당사항 없음`이 있어야 한다. | N/A 행 비교 결과 |
| 17-1 | SW저작권확인서 폴더 탐색 | `인증관련` 폴더 아래에서 검사한다. | 선택된 폴더 |
| 17-2 | SW저작권확인서 파일명 | 파일명에 `확인서`가 포함되어야 한다. | 매칭 파일명 목록 |
| 17-3 | SW저작권확인서 형식/개수 | `.pdf` 파일이 1개 이상 있어야 한다. | 실제 PDF 개수 |
| 18-1 | 홍보이미지 폴더 탐색 | `홍보` 폴더 아래에서 검사한다. | 선택된 폴더 |
| 18-2 | 한글/워드/이미지 존재 (OR) | `예시`가 포함되지 않은 파일명의 한글(`.hwp`, `.hwpx`)/워드(`.doc`, `.docx`, `.docm`) 파일이 있거나, 이미지 파일이 1개 이상 있어야 한다. | 한글/워드/이미지 파일 목록 |
| 18-3 | 한글/워드 파일명 금지어 | 이미지 파일이 없을 때만, 한글/워드 파일명에 `예시`가 포함되지 않아야 한다(이미지 파일은 금지어 검사 대상 아님). | 금지어 매칭 한글/워드 파일 목록 |

## 1. 계약서

- 상태: `구현됨`
- code: `artifact_01`
- rule_type: `required_artifact_file`
- 점검 대상 폴더: `계약` 단어가 포함된 폴더
- 파일명 조건: `계약서`, `{프로젝트번호}` 포함
- 확장자/개수 조건: `.pdf` 정확히 1개
- 문서 내부 검사 조건: 없음
- 변수 사용 목록: `{프로젝트번호}`
- 실패 메시지: `파일이 없습니다.`
- 저장 산출물: 없음
- 산출물 결과 키: `계약서`
- 구현 메모: 현재 seed 규칙은 `{project_number}` 표기를 사용하지만 의미는 `{프로젝트번호}`와 같다.

## 2. 합의서

- 상태: `구현됨`
- code: `artifact_02`
- rule_type: `document_artifact_check`
- 점검 대상 폴더: `계약` 단어가 포함된 폴더
- 파일명 조건: `합의서`, `{프로젝트번호}` 포함
- 확장자/개수 조건: Word 파일(`.docx`, `.docm`, `.doc`) 1개, `.pdf` 1개
- 문서 내부 검사 조건:
  - Word 표에서 `시험신청번호` 오른쪽 셀이 `{프로젝트번호}`와 일치해야 한다.
  - `.pdf` 1페이지에서 `시험신청번호` 주변 텍스트에 `{프로젝트번호}`가 포함되어야 한다.
  - Word 머리글에 `{프로젝트번호}`가 작성되어 있어야 한다.
  - Word 바닥글에 `TIS-0101-3 (00)`이 작성되어 있어야 한다.
- 변수 사용 목록: `{프로젝트번호}`
- 실패 메시지:
  - 파일 누락: `필요한 합의서 파일이 없습니다.`
  - 내부값 불일치: `프로젝트 번호가 맞지 않습니다.`
  - 머리글 프로젝트번호 오류: `합의서 머리말에 프로젝트 번호가 잘못 작성됨`
  - 바닥글 양식번호 오류: `합의서 바닥글에 양식번호가 잘못 작성됨`
- 저장 산출물: `.pdf` 1페이지 캡처 이미지
- 산출물 결과 키: `합의서(PDF)`
- 구현 메모:
  - 파일/내용 검사와 PDF 캡처 저장/UI 조회 버튼이 구현되어 있다.
  - Word 머리글/바닥글은 섹션별 `word/header*.xml`, `word/footer*.xml` 전체를 합쳐 검사한다. 첫 페이지와 짝/홀수 페이지 설정이 분리된 문서도 같은 기준으로 본다.
  - 실제 사용 시 머리글이 이미지나 필드 코드로만 들어간 경우 텍스트 추출이 되지 않아 실패할 수 있다. 운영 템플릿은 텍스트 머리글/바닥글을 유지하는 것을 권장한다.

## 3. 수수료산정표

- 상태: `구현됨`
- code: `artifact_03`
- rule_type: `required_artifact_file`
- 점검 대상 폴더: `계약` 단어가 포함된 폴더
- 파일명 조건: `수수료산정표`, `{프로젝트번호}` 포함
- 확장자/개수 조건: `.xlsx` 정확히 1개
- 문서 내부 검사 조건: 없음
- 변수 사용 목록: `{프로젝트번호}`
- 실패 메시지: `파일이 없습니다.`
- 저장 산출물: 없음
- 산출물 결과 키: `수수료산정표`
- 구현 메모: 현재 `.xlsx`만 허용한다.

## 4. 시험환경구성도

- 상태: `구현됨`
- code: `artifact_04`
- rule_type: `required_artifact_file`
- 점검 대상 폴더: `시험` 단어가 포함된 폴더 하위의 `계획` 단어가 포함된 폴더
- 파일명 조건: `구성도`, `{프로젝트번호}` 포함
- 확장자/개수 조건: `.png` 또는 `.pptx` 1개 이상
- 문서 내부 검사 조건: 없음
- 변수 사용 목록: `{프로젝트번호}`
- 실패 메시지:
  - 폴더 누락: `시험계획 폴더를 찾을 수 없습니다`
  - 파일 누락: `시험환경구성도 파일을 찾을 수 없습니다`
  - 확장자 오류: `시험환경구성도 파일이 png/pptx가 아닙니다`
- 저장 산출물: 없음
- 산출물 결과 키: `시험환경구성도`
- 구현 메모: `{프로젝트번호}`와 `구성도`가 포함된 `.png` 또는 `.pptx`가 1개 이상 있으면 통과한다.

## 5. 품질특성별제품정보기재사항

- 상태: `구현됨`
- code: `artifact_05`
- rule_type: `document_artifact_check`
- 점검 대상 폴더: `시험` 단어가 포함된 폴더 하위의 `계획` 단어가 포함된 폴더
- 파일명 조건: `품질특성별`, `{프로젝트번호}` 포함
- 확장자/개수 조건: Word 파일(`.docx`, `.docm`, `.doc`) 정확히 1개
- 문서 내부 검사 조건:
  - 문서 본문에 `{프로젝트번호}`와 `품질특성별` 문구가 함께 있어야 한다.
  - 위 문구 다음 문단이 날짜 형식이어야 한다.
  - 바닥글에 `TPG` 또는 `TIS` 서식번호 문구가 없어야 한다.
- 변수 사용 목록: `{프로젝트번호}`
- 실패 메시지:
  - 폴더 누락: `시험계획 폴더를 찾을 수 없습니다`
  - 파일명/파일 누락: `품질특성별제품정보기재사항 파일을 찾을 수 없습니다`
  - 제목 오류: `1페이지 제목에 프로젝트번호와 품질특성별 문구가 필요합니다.`
  - 날짜 오류: `1페이지 날짜가 잘못되었습니다.`
  - 바닥글 서식번호 오류: `바닥글에 서식번호가 작성되면 안됨`
- 저장 산출물: 없음
- 산출물 결과 키: `품질특성별제품정보기재사항`
- 구현 메모: 날짜는 `yyyy-mm-dd`, `yyyy.mm.dd.`, `yyyy년 m월 d일`, 연도 생략 표기, 요일/괄호 표기까지 폭넓게 허용한다.

## 6. 기능리스트

- 상태: `구현됨`
- code: `artifact_06`
- rule_type: `excel_feature_list_check`
- 점검 대상 폴더: `시험` 단어가 포함된 폴더 하위의 `계획` 단어가 포함된 폴더
- 파일명 조건: `기능`, `{프로젝트번호}` 포함
- 확장자/개수 조건: `.xls` 또는 `.xlsx`, 정확히 1개
- 문서 내부 검사 조건:
  - 해당 Excel 파일의 시트가 정확히 1개여야 한다.
  - 파일 내에 `{프로젝트번호} 기능리스트` 문장이 있어야 한다.
  - `작성자`라는 단어가 작성된 같은 셀에 `{PL}`이 포함되어야 한다.
  - A열에서 `대분류`가 작성된 셀을 찾고, 그 셀 기준 오른쪽과 아래로 값이 있는 모든 영역을 캡처한다.
  - Excel 머리글에 `TTA`가 없어야 한다.
  - Excel 바닥글에 `TPG` 또는 `TIS`가 없어야 한다.
- 변수 사용 목록: `{프로젝트번호}`, `{PL}`
- 실패 메시지:
  - 폴더 누락: `시험계획 폴더를 찾을 수 없습니다`
  - 파일 누락: `기능리스트 파일을 찾을 수 없습니다`
  - 확장자 오류: `기능리스트 파일이 xlsx/xls가 아닙니다`
  - 시트 2개 이상: `불필요한 시트가 존재`
  - 시험번호 또는 작성자 오류: `시험번호 또는 작성자가 잘못 작성됨`
  - 머리글 오류: `머리글에 프로젝트번호가 작성되면 안됨`
  - 바닥글 서식번호 오류: `바닥글에 서식번호가 작성되면 안됨`
- 저장 산출물: `대분류` 기준 표 영역 스크린샷
- 산출물 결과 키: `기능리스트`
- 구현 메모:
  - `.xlsx`는 `openpyxl`, `.xls`는 `xlrd`로 읽는다.
  - 시트 수, 제목, 작성자, `대분류` 기준 영역 탐색은 구현됐다.
  - `대분류` 기준 영역은 `raw_detail_json.capture_area`에 메타데이터를 저장하고, 같은 영역을 표 이미지로 렌더링해 artifact API/UI 버튼으로 조회할 수 있게 저장한다.
  - 머리글/바닥글 검사는 `.xlsx` 인쇄 설정과 `.xls` BIFF HEADER/FOOTER 레코드를 함께 사용한다.

## 7. 제품 스크린샷

- 상태: `구현됨`
- code: `artifact_08`
- rule_type: `image_screenshot_folder_date_check`
- 점검 대상 폴더: `설계` 단어가 포함된 폴더 하위
- 폴더 탐색 조건:
  - `설계` 폴더 하위에서 이미지 파일이 5개 이상 들어있는 폴더를 B/C 후보로 본다.
  - B/C 후보 폴더가 2개 이상 있는 부모 폴더를 A 폴더로 본다.
  - A, B, C 폴더명은 제각각이므로 이름으로 판단하지 않는다.
- 파일명 조건: 없음
- 확장자/개수 조건:
  - 이미지 확장자는 `.png`, `.jpg`, `.jpeg`, `.bmp`, `.gif`만 대상으로 한다.
  - B/C 후보 폴더에는 각각 이미지 파일이 5개 이상 있어야 한다.
- 문서 내부 검사 조건:
  - B/C 후보 폴더 안의 이미지 파일 수정일자가 `{시작일}`부터 `{종료일}` 사이에 있어야 한다.
  - zip 내부 파일은 실제 Windows 생성일이 안정적으로 보존되지 않으므로 zip entry의 수정일자를 기준으로 한다.
  - 날짜 범위는 양 끝 날짜를 포함한다. 즉, 시작일 00:00:00부터 종료일 23:59:59까지 통과한다.
- 변수 사용 목록: `{시작일}`, `{종료일}`
- 실패 메시지:
  - 폴더 탐색 실패: `제품 스크린샷 폴더를 찾을 수 없음`
  - 이미지 개수 부족: `제품 스크린샷 이미지가 부족함`
  - 날짜 범위 오류: `시험기간은 {시작일}~{종료일}인데 수정일자가 {범위밖수정일자목록}인 이미지가 {개수}개 존재함`
- 저장 산출물: 날짜 범위 밖 이미지 목록과 후보 폴더 경로를 `raw_detail_json`에 저장
- 산출물 결과 키: `최초/최종형상RawData`
- 구현 메모:
  - zip entry 수정일자는 zip 파일 내부 메타데이터를 사용한다.
  - 다운로드 폴더를 직접 검사하는 경우에는 파일 시스템 수정일자를 사용한다.
  - 실제 생성일은 OS/zip 생성 방식에 따라 보존되지 않을 수 있으므로 사용하지 않는다.

## 8. 시험성적서

- 상태: `구현됨`
- code: `artifact_13`
- rule_type: `test_report_document_check`
- 점검 대상 폴더: `시험` 단어가 포함된 폴더 하위의 `종료` 단어가 포함된 폴더
- 파일명 조건: `시험성적서`, `{프로젝트번호}` 포함
- 확장자/개수 조건: Word 파일(`.docx`, `.docm`, `.doc`) 1개, `.pdf` 1개
- 문서 내부 검사 조건:
  - `.pdf` 파일의 1페이지를 캡처해 웹페이지 버튼으로 조회할 수 있게 저장한다.
  - 9번 시험계획서의 `<세부사양>` 표 비교 대상은 시험성적서에서 `<세부사양>` 문단 또는 셀 이후 가장 가까운 첫 번째 표로 한다.
  - Word 파일에서 `결함리포트 송부` 단어가 포함된 표의 셀을 찾는다.
  - 해당 셀에서 `1차: yyyy.mm.dd` 날짜를 찾아 `{1차}` 변수에 저장한다.
  - 해당 셀에서 `2차: yyyy.mm.dd` 날짜를 찾아 `{2차}` 변수에 저장한다.
  - `3차`, `4차`가 있을 경우 같은 방식으로 `{3차}`, `{4차}` 변수에 저장한다.
  - 산출된 차수별 보고일자 개수를 `{결함차수}`로 본다.
- 변수 사용 목록: `{프로젝트번호}`, `{결함차수}`, `{1차}`, `{2차}`, `{3차}`, `{4차}`
- 실패 메시지: 파일이 없다는 메시지 표시
- 저장 산출물: `.pdf` 1페이지 캡처 이미지
- 산출물 결과 키: `시험성적서(PDF)`
- 구현 메모:
  - `결함리포트 송부` 표에서 산출한 `{결함차수}`, `{1차}`, `{2차}` 등은 10번 결함리포트 파일 개수/보고일자 검사에 사용한다.
  - `결함리포트 송부` 단어가 포함된 표 전체 텍스트에서 `N차: yyyy.mm.dd` 형식의 날짜를 추출한다.
  - `.pdf` 1페이지 캡처 이미지는 artifact 저장소에 PNG로 저장하고, 규칙 결과 상세 팝업에서 버튼으로 조회한다.
  - `<세부사양>` 표는 `raw_detail_json.spec_table`과 `raw_detail_json.variables["시험성적서_세부사양표"]`에 저장하며, 9번 시험계획서가 후속 규칙으로 읽어 비교한다.

## 9. 시험계획서

- 상태: `구현됨`
- code: `artifact_07`
- rule_type: `test_plan_document_check`
- 점검 대상 폴더: `시험` 단어가 포함된 폴더 하위의 `계획` 단어가 포함된 폴더
- 파일명 조건: `계획서`, `{프로젝트번호}` 포함
- 확장자/개수 조건: Word 파일(`.docx`, `.docm`, `.doc`) 1개, `.pdf` 1개
- 문서 내부 검사 조건:
  - `.docx` 첫 번째 표의 1행 2열 값이 `{시작일}`과 정확히 일치해야 한다.
  - 첫 번째 표의 3행 2열 값에 센터별 담당자 expected 값이 포함되어야 한다.
  - 첫 번째 표의 4행 2열 값에 `{PL}`이 포함되어야 한다.
  - `.docx` 두 번째 표에서 `소프트웨어 명` 오른쪽 칸은 `{제품명}`(국문명) 또는 그 영문명 병기값(있는 경우) 중 하나와 일치해야 한다.
  - 같은 표에서 `버전` 오른쪽 칸은 `{버전}`과 일치해야 한다. 숫자 버전은 `v`, `ver` 같은 접두사가 있어도 없어도 같은 버전으로 보고, 문자 버전은 공백과 대소문자를 무시해 비교한다.
  - 같은 표에서 `시험신청번호` 오른쪽 칸은 `{프로젝트번호}`와 일치해야 한다.
  - `5.1 형상항목 식별 규칙` 문장 다음에 나오는 표에서 `형상항목 ID`가 작성된 열을 찾고, 헤더 아래 비어 있지 않은 모든 값에 `{프로젝트번호}`가 포함되어야 한다.
  - `2.2 시험일정` 문장 다음에 나오는 표에서 `WD`라고 작성된 열의 값이 위에서부터 `1`, `1`, `{WD}-3`, `1`이어야 한다.
  - `.docx` 바닥글에 `Copyright {연도} TTA`가 정확히 포함되어야 한다.
  - `.docx` 바닥글에 `TIS-`, `TPG`, `TIS` 문자열이 없어야 한다.
  - `.docx` 바닥글에 `소프트웨어시험인증연구소`라는 단어가 없어야 한다.
  - `<세부사양>` 다음 표는 8번 시험성적서의 `<세부사양>` 다음 표와 일치해야 한다.
- 변수 사용 목록: `{프로젝트번호}`, `{제품명}`, `{버전}`, `{시작일}`, `{PL}`, `{WD}`, `{연도}`
- 실패 메시지:
  - 파일 누락: 존재하지 않는 파일 통보
  - 첫 번째 표 날짜 오류: `시험계획서 날짜가 잘못 작성됨`
  - 담당자 오류: `시험계획서 담당자가 잘못 작성됨`
  - PL 오류: `시험계획서 PL이 잘못 작성됨`
  - 제품정보 오류: `제품정보가 틀림`
  - 형상항목 ID 오류: `형상항목 ID가 잘못 작성됨`
  - 시험일정 WD 오류: `시험일정 WD가 틀림`
  - 바닥글 오류: `바닥글 Copyright가 잘못 작성됨`
  - 바닥글 양식번호 오류: `시험계획서 바닥글에 양식번호가 잘못 작성됨`
  - 바닥글 서식번호 오류: `바닥글에 서식번호가 작성되면 안됨`
  - 바닥글 기관명 오류: `시험계획서 바닥글에 '소프트웨어시험인증연구소'라는 단어가 잘못 작성됨`
  - 세부사양 비교 오류: `시험환경 세부사양 표가 결과서와 다름`
- 저장 산출물: `.pdf` 1페이지 캡처 이미지
- 산출물 결과 키: `시험계획서(PDF)`
- 구현 메모:
  - 첫 번째 표의 좌표는 사람이 보는 기준인 1행 1열부터 계산한다.
  - 담당자 expected 값은 센터별 매핑을 우선 사용한다. 현재 기준은 분당 `임우섭`, 상암 `김진영`, 영남 `이재훈`, 기본 `김진영`이다.
  - `Copyright {연도} TTA` 검사는 대소문자와 공백을 모두 정확히 비교한다.
  - 바닥글 금지어는 `TIS-`, `TPG`, `TIS`, `소프트웨어시험인증연구소`를 검사한다.
  - 8번 시험성적서가 `raw_detail_json.variables["시험성적서_세부사양표"]`에 표를 저장하고, 9번은 seed `sort_order=96`으로 8번 바로 뒤에서 해당 표와 결정적 비교를 수행한다.
  - `<세부사양>` 표 비교는 셀의 공백/줄바꿈을 제거한 행렬 완전 일치로 판정한다. LLM 의미 비교는 추후 보조 검토로만 붙일 수 있다.

## 10. 결함리포트

- 상태: `구현됨`
- code: `artifact_10`
- rule_type: `defect_report_check`
- 점검 대상 폴더: `수행` 단어가 포함된 폴더 하위
- 파일명 조건:
  - `결함리포트`, `{프로젝트번호}` 포함
  - 중복 개정 정리 후 `{결함차수} + 1`개 파일이 있어야 한다.
  - 파일 버전은 major 차수 기준으로 `v1.x`부터 `v{결함차수 + 1}.x`까지 누락 없이 관리되어야 한다.
  - 같은 major의 minor 개정이 여러 개 있으면 최신 minor만 검사한다. 예: `v1.0`, `v1.1`, `v2.0`이면 `v1.1`, `v2.0`을 사용한다.
  - 같은 major/minor가 여러 개 있으면 파일 수정일이 가장 최신인 파일을 사용한다.
  - 예: `{결함차수}=2`이면 major 1, 2, 3 파일이 각각 1개씩 있어야 한다.
  - 실제 결함리포트 파일 수는 중복 개정 정리 후 `{결함차수} + 1`이어야 한다.
- 확장자/개수 조건: Excel 파일 전체 허용. `.xls` 또는 `.xlsx`
- 문서 내부 검사 조건:
  - major 1 파일에는 `1차 결함리포트` 시트가 있어야 한다.
  - major 2 파일에는 `1차 결함리포트`, `2차 결함리포트` 시트가 있어야 한다.
  - 마지막 버전 파일에는 이전 차수 결함리포트 시트 전체와 `최종결함리포트`, `시험분석자료` 시트가 있어야 한다.
  - 차수별 시트명 비교는 공백을 제거한 뒤 수행한다. 예: `1차 결함리포트`와 `1차결함리포트`는 같은 시트명으로 본다.
  - 예: 마지막 major가 3이면 `1차 결함리포트`, `2차 결함리포트`, `최종결함리포트`, `시험분석자료` 시트가 있어야 한다.
  - 예: 마지막 major가 4이면 major 3 파일에는 `1차 결함리포트`, `2차 결함리포트`, `3차 결함리포트` 시트가 있고, major 4 파일에는 `1차 결함리포트`, `2차 결함리포트`, `3차 결함리포트`, `최종결함리포트`, `시험분석자료` 시트가 있어야 한다.
  - 각 파일별 모든 시트의 1~3행 중 `시험환경` 단어가 포함된 셀을 찾아 모두 동일한 값을 가져야 한다.
  - 각 파일별 모든 시트의 머리글에 `프로젝트번호`라는 단어가 없어야 한다.
  - 각 파일별 모든 시트의 바닥글에 `소프트웨어시험인증연구소`라는 단어가 없어야 한다.
  - 각 파일별 모든 시트의 1~3행 중 `{프로젝트번호} {시트명}`으로 작성된 값을 찾아야 한다. `N차 결함리포트` 시트는 표지에 `N차`가 빠진 `결함리포트`만 적힌 서식도 허용한다. 표지 제목 확인과 보고일자 확인은 서로 다른 세부항목으로 분리되어 있어, 표지 제목이 틀렸는데 우연히 날짜만 일치하는 경우에도 부적합 사유가 표지 제목 문제임을 화면에서 바로 알 수 있다.
  - 위 셀의 `보고일자: yyyy년 mm월 dd일` 날짜는 차수별 변수와 일치해야 한다.
  - `1차 결함리포트` 시트는 `{1차}`, `2차 결함리포트` 시트는 `{2차}`처럼 차수별 보고일자와 비교한다.
  - `최종결함리포트`와 `시험분석자료`는 `{종료일}`과 비교한다. `시험분석자료`도 반드시 시트명 그대로(`시험분석자료`)가 표지 제목으로 적혀 있어야 하며, `최종결함리포트`와 같은 표지 문구(`최종 결함리포트`)를 재사용한 경우 표지 제목 세부항목이 부적합 처리된다.
  - 마지막 버전 결함리포트 파일의 `최종결함리포트` 시트에서 B5셀부터 아래까지 값이 있는 셀 개수를 `{잔여결함수}`로 저장한다. 값이 없으면 0으로 저장한다.
  - 마지막 버전 결함리포트 파일의 `시험분석자료` 시트에서 결함 목록(7행~)을 순회하며, E열(결함정도)이 정확히 `H`이고 같은 행 C열(시험환경)이 빈 값이나 `-`가 아닌 행의 개수를 세어 `{H}`로 저장한다(하단 "결함정도별 결함내역" 요약표의 라벨 셀을 찾는 방식이 아니다).
  - 같은 시트에서 C~F열의 `수정전` 값을 찾고, 5개 아래 셀의 숫자를 `{R}`로 저장한다.
- 변수 사용 목록: `{프로젝트번호}`, `{결함차수}`, `{1차}`, `{2차}`, `{종료일}`, `{잔여결함수}`, `{H}`, `{R}`
- 실패 메시지:
  - 시험성적서 기준 결함차수와 파일 개수 불일치: `시험성적서의 결함 차수와 결함리포트 개수가 다름`
  - 파일명/버전/개수 오류: `결함리포트 파일명이 잘못됨`
  - 시트 구성 오류: `{파일명}에 시트가 잘못 작성됨`
  - 시험환경 비교 불가 또는 불일치: `시험환경 정보 잘못 작성됨`
  - 머리글 프로젝트번호 잔존: `결함리포트 머리글에 프로젝트번호 삭제`
  - 바닥글 기관명 오류: `결함리포트 바닥글에 '소프트웨어시험인증연구소'라는 단어가 잘못 작성됨`
  - 프로젝트 번호, 차수, 보고일자 오류: `프로젝트 번호, 결함 차시, 보고일자 중 잘못된 값이 작성됨`
- 저장 산출물:
  - 버전별 파일 목록과 누락 버전 목록
  - 파일별 시트 검사 결과
  - 시험환경 비교값
  - `{잔여결함수}`, `{H}`, `{R}` 산출 근거 셀 위치
- 산출물 결과 키: `결함리포트`
- 구현 메모:
  - `{결함차수}`, `{1차}`, `{2차}` 등 보고일자 변수는 8번 시험성적서 규칙에서 산출한다.
  - 8번 시험성적서 실제 규칙의 `sort_order`는 95로 고정되어, 10번 결함리포트 실제 규칙이 구현될 때 기본 `sort_order=100`보다 먼저 실행된다.
  - `{종료일}`은 `yyyy.mm.dd.` 형식이고, 결함리포트 보고일자는 `yyyy년 mm월 dd일` 형식이므로 날짜 파싱 후 같은 날짜인지 비교한다.
  - 마지막 버전은 중복 개정을 정리한 뒤 `{결함차수} + 1` major에 해당하는 파일로 본다.
  - 각 버전의 시트 구성은 기대 시트와 정확히 일치해야 하며, 추가 시트가 있어도 실패 처리한다.
  - 머리글/바닥글 검사는 모든 버전 파일의 모든 시트에 적용한다. 숨김 시트가 포함된 파일도 현재 파서 기준으로 검사 대상이므로, 운영 템플릿에 숨김 시트를 남기는 경우 오탐 가능성이 있다.
  - `프로젝트번호` 금지 검사는 현재 정확한 부분 문자열 기준이다. `프로젝트 번호`처럼 띄어 쓴 변형까지 잡아야 한다면 별도 정규화 조건을 추가한다.
  - 11번 테스트케이스의 `{잔여결함수}`와 12번 점검표의 `{H}`, `{R}`은 이 규칙 산출값을 사용한다.
  - 10-11/10-12(품질특성별/결함정도별 결함내역 일치)는 최종 버전 파일의 `시험분석자료` 시트 하나만으로 검사한다. 결함 목록 시작 행은 고정 행 번호가 아니라 G열의 `품질특성`/E열의 `결함정도` 헤더 셀을 찾아 그 바로 다음 행부터로 보며, 회차 구분 없이 연속값 전체(모든 회차 누적)를 기준으로 요약표와 비교한다.

## 11. 테스트케이스

- 상태: `구현됨`
- code: `artifact_09`
- rule_type: `test_case_check`
- 점검 대상 폴더: `설계` 단어가 포함된 폴더 하위
- 파일명 조건: `테스트케이스`, `{프로젝트번호}` 포함
- 확장자/개수 조건: Excel 파일 전체 허용. `.xls` 또는 `.xlsx`, 정확히 1개
- 문서 내부 검사 조건:
  - 해당 Excel 파일의 시트가 정확히 1개여야 한다.
  - 파일 내에 `{프로젝트번호} 테스트케이스`라는 문장이 있어야 한다.
  - 파일 내에 `작성자:`와 `{PL}`이 포함된 셀이 있어야 한다.
  - 위 작성자 셀 바로 아래 셀에 `검토자:`와 센터별 검토자 expected 값이 포함되어야 한다.
  - 파일 내에 `작성일: {시작일} ~ {종료일}` 값이 있어야 한다.
  - 작성일 비교는 공백을 제거한 뒤 비교한다.
  - `상세 테스트 결과` 값이 작성된 열에서 `F` 값의 개수가 `{잔여결함수}`와 일치해야 한다.
  - Excel 머리글에 `TTA`가 없어야 한다.
  - Excel 바닥글에 `TPG`, `TIS`, `소프트웨어시험인증연구소`가 없어야 한다.
  - Excel 바닥글에 `TTA`가 포함되어야 한다.
- 변수 사용 목록: `{프로젝트번호}`, `{PL}`, `{시작일}`, `{종료일}`, `{잔여결함수}`
- 실패 메시지:
  - 파일 누락: `파일이 존재하지 않음`
  - 시트 개수 오류: `테스트케이스 시트가 1개 이상임`
  - 프로젝트 번호 오류: `프로젝트 번호가 잘못 작성됨`
  - 작성자/검토자 오류: `작성자 또는 검토자가 잘못 작성됨`
  - 작성일 오류: `작성일이 잘못 작성됨`
  - 잔여 결함 개수 오류: `잔여 결함이 작성되지 않음`
  - 머리글 오류: `머리글에 프로젝트번호가 작성되면 안됨`
  - 바닥글 서식번호 오류: `바닥글에 서식번호가 작성되면 안됨`
  - 바닥글 기관명 오류: `테스트케이스 바닥글에 '소프트웨어시험인증연구소'라는 단어가 잘못 작성됨`
  - 바닥글 TTA 누락: `바닥글에 TTA가 누락됨`
- 저장 산출물: `상세 테스트 결과` 열의 `F` 개수와 해당 행 정보를 `raw_detail_json`에 저장
- 산출물 결과 키: `테스트케이스`
- 구현 메모:
  - 작성자/검토자는 정확 비교가 아니라 포함 여부로 판정한다.
  - 검토자 expected 값은 센터별 매핑을 우선 사용한다. 현재 기준은 분당 `임우섭`, 상암 `김진영`, 영남 `이재훈`, 기본 `김진영`이다.
  - 작성일은 날짜 문자열의 공백 제거 후 비교한다. 날짜 자체는 `yyyy.mm.dd.` 형식을 기준으로 한다.
  - `{잔여결함수}`는 10번 결함리포트 규칙에서 산출하므로 seed `sort_order=105`로 10번 뒤에 실행한다.
  - `상세 테스트 결과` 헤더 셀 아래 같은 열에서 값이 정확히 `F`인 셀 개수를 센다. 비교 시 셀 값의 공백은 제거하고 대문자로 변환한다.
  - Excel 바닥글은 `.xlsx`의 odd/even/first footer 전체와 `.xls`의 BIFF FOOTER(0x15) 레코드를 합쳐 검사한다. 바닥글이 비어 있으면 금지어 검사는 통과한다.

## 12. 점검표

- 상태: `구현됨`
- code: `artifact_11`
- rule_type: `inspection_checklist_check`
- 점검 대상 폴더: `설계` 단어가 포함된 폴더 하위
- 파일명 조건: `점검표`, `{프로젝트번호}` 포함
- 확장자/개수 조건: Excel 파일 전체 허용. `.xls` 또는 `.xlsx`, 정확히 1개. PDF도 정확히 1개만 존재해야 하며 해당 PDF를 캡처한다.
- 문서 내부 검사 조건:
  - 모든 시트의 머리글에 `프로젝트번호: {프로젝트번호}`가 정확히 작성되어 있어야 한다.
  - 모든 시트의 바닥글에 `TIS-`, `TPG`, `TIS`, `소프트웨어시험인증연구소` 문자열이 없어야 한다.
  - 모든 시트의 바닥글에 `한국정보통신기술협회`라는 단어가 있어야 한다.
  - `표지` 시트에 `{프로젝트번호}`와 `점검표` 단어가 함께 작성된 셀이 있어야 한다.
  - `표지` 시트에 `{시작일} ~ {종료일}` 기간이 작성된 셀이 있어야 한다. 날짜 구분자(`.`, `-`, `/`, `년월일`)는 형식이 달라도 같은 날짜이면 통과한다.
  - `표지` 시트에서 센터별 검토자 expected 값과 `{PL}`(작성자)을 각각 다른 셀에서 찾아도 된다. 두 이름이 같은 셀이 아니어도 각각 한 셀씩 존재하면 통과하며, 공백 제거 후 비교한다.
  - `기능별 점검표` 시트의 A8셀의 `1` 숫자부터 A열 아래로 1씩 증가하는 연속 숫자를 확인하고, 마지막 연속 숫자가 작성된 행 번호를 임시 변수 A로 저장한다. 숫자가 끝난 뒤 합계/요약 같은 비숫자 행이 와도 마지막 숫자 행까지를 A로 본다.
  - `기능별 점검표` 시트의 E8셀부터 AI{A}셀까지 모든 값이 입력되어 있어야 한다.
  - `2. 기능적합성` 시트의 C열 마지막 값이 있는 행 번호를 임시 변수 A로 저장한다.
  - `2. 기능적합성` 시트의 A16셀부터 C{A}셀까지의 값은 `기능별 점검표` 시트의 B8셀부터 D{A}셀까지의 값과 동일해야 한다.
  - `2. 기능적합성` 시트의 D16셀부터 D열의 마지막 행까지 빈 값이 없어야 한다.
  - `6. 신뢰성` 시트의 C5셀 값이 `{WD}`와 동일해야 한다.
  - `6. 신뢰성` 시트의 C11셀은 `{H}`, E11셀은 `{R}` 값과 동일해야 한다.
  - `.pdf` 점검표 파일이 있어야 한다.
  - `.pdf` 점검표 파일의 1페이지를 캡처해 웹페이지 버튼으로 조회할 수 있게 저장한다.
  - `측정항목별 점수표` 시트의 D7~D90 값을 `{측정항목별점수표}` 변수에 저장한다.
- 변수 사용 목록: `{프로젝트번호}`, `{시작일}`, `{종료일}`, `{PL}`, `{WD}`, `{H}`, `{R}`, `{측정항목별점수표}`
- 실패 메시지:
  - 파일 누락: `파일이 존재하지 않음`
  - 머리글 오류: `머리글(프로젝트번호)이 잘못 작성됨`
  - 바닥글 양식번호 오류: `점검표 바닥글에 양식번호가 잘못 작성됨`
  - 바닥글 서식번호 오류: `바닥글에 서식번호가 작성되면 안됨`
  - 바닥글 기관명 오류: `바닥글에 '소프트웨어시험인증연구소'가 작성되면 안됨`
  - 바닥글 협회명 누락: `점검표 바닥글에 '한국정보통신기술협회'라는 단어가 누락됨`
  - 표지 제목 오류: `표지 제목이 잘못 작성됨`
  - 표지 날짜 오류: `표지 날짜가 잘못 작성됨`
  - 표지 작성자 오류: `표지 작성자가 잘못 작성됨`
  - 기능별 점검표 빈 셀 오류: `기능별 점검표 시트에 빈 셀이 확인됨`
  - 기능적합성 표 내용 불일치: `기능적합성 시트의 2.3번 기능표 내용 확인 필요함`
  - 기능적합성 결과값 미작성: `기능적합성 시트의 기능표 결과값 미작성`
  - 신뢰성 WD 오류: `신뢰성 시트의 WD 잘못 작성함`
  - 신뢰성 결함 개수 오류: `신뢰성 시트의 결함 개수가 잘못 작성됨`
  - PDF 파일 누락: `점검표 pdf 파일이 없음`
- 저장 산출물:
  - 빈 셀 위치 목록
  - 기능표 비교 불일치 위치
  - 신뢰성 시트의 `{WD}`, `{H}`, `{R}` 실제/기대값
  - `.pdf` 1페이지 캡처 이미지
  - `{측정항목별점수표}` 산출값
- 산출물 결과 키: `점검표(PDF)`
- 구현 메모:
  - 기존 DB 컬럼명이 `점검표(PDF)`이므로 Excel 파일 검사라도 해당 컬럼에 write-back한다.
  - 표지 검토자 expected 값은 센터별 매핑을 우선 사용한다. 현재 기준은 분당 `임우섭`, 상암 `김진영`, 영남 `이재훈`, 기본 `김진영`이다.
  - `{H}`와 `{R}`은 10번 결함리포트 규칙 산출값이므로, 12번 구현 시 해당 값을 읽어 연결한다.
  - Excel 검사와 PDF 존재/캡처 검사를 같은 12번 규칙에서 관리한다.
  - Excel 머리글은 `.xlsx`는 openpyxl 인쇄 머리글로 검사하고, `.xls`는 xlrd가 인쇄 머리글을 노출하지 않으므로 워크북 스트림의 BIFF HEADER(0x14) 레코드를 직접 파싱해 시트별 머리글을 읽는다. 두 경우 모두 실패 시 시트 상단 1~3행에서 같은 문구를 보조로 찾는다.
  - Excel 바닥글은 `.xlsx`의 odd/even/first footer 전체와 `.xls`의 BIFF FOOTER(0x15) 레코드를 합쳐 검사한다.
  - 바닥글 금지어는 `TIS-`, `TPG`, `TIS`, `소프트웨어시험인증연구소`를 검사한다. `한국정보통신기술협회`는 필수어로 검사한다.

## 13. 1차/2차/성능/보안RawData

- 상태: `구현됨`
- code: `artifact_12`
- rule_type: `rawdata_folder_structure_check`
- 점검 대상 폴더: `수행` 단어가 포함된 폴더
- 파일명 조건: 없음
- 확장자/개수 조건: 없음
- 문서 내부 검사 조건:
  - `수행` 폴더 안에 `결함` 단어가 포함된 폴더가 있어야 한다. (`결함리포트`, `결함 스크린샷` 등 명칭이 달라도 인정)
  - `결함` 폴더 안에는 이미지 파일이 1개 이상 있어야 한다.
  - `수행` 폴더 안에 `보안` 단어가 포함된 폴더가 있어야 한다.
  - `보안` 폴더 안에는 하위 폴더가 2개 이상 있어야 한다.
  - `보안`의 각 하위 폴더 안에는 파일 또는 폴더가 1개 이상 있어야 한다.
  - 단일 폴더로 한 번 더 감싸진 보안 rawdata는 내부 폴더를 풀어서 검사한다.
  - 파일명에 `보안성`, `인빅티`, `invicti`, `수행`, `대상`, `시험`, `면제` 중 하나라도 포함된 파일이 있으면 보안 rawdata 존재로 인정할 수 있다(OR 매칭. 예: "인빅티 대상아님" 0바이트 안내 파일도 인정).
  - 보안 폴더에 하위 폴더 없이 txt 안내 파일만 있으면 예외적으로 보안 rawdata 존재로 인정한다.
  - `수행` 폴더 안에 `성능` 단어가 포함된 폴더가 있어야 한다.
  - `성능` 폴더 안에는 하위 항목(파일 또는 폴더)이 1개 이상 있어야 한다. (rawdata 하위 구성이 프로젝트마다 달라 개수는 고정하지 않는다)
- 변수 사용 목록: 없음
- 실패 메시지:
  - 결함리포트 rawdata 폴더 누락: `결함리포트 rawdata 확인 불가`
  - 보안 폴더 구조 오류: `보안성 rawdata 확인 불가`
  - 성능 폴더 구조 오류: `성능 rawdata 확인 불가`
- 저장 산출물: 탐색된 rawdata 폴더 경로와 각 하위 폴더 항목 개수
- 산출물 결과 키: `1차/2차/성능/보안RawData`
- 구현 메모: rawdata zip만 다운로드된 경우에도 이 규칙은 실행된다. `raw_data.zip`, `raw-data.zip`, 중첩 zip도 rawdata 후보로 인식한다.

## 14. 시험기록서

- 상태: `구현됨`
- code: `artifact_14`
- rule_type: `downloadable_artifact_check`
- 점검 대상 폴더: 전체 다운로드/검사 대상에서 검색한다.
- 파일명 조건: `기록서` 포함
- 확장자/개수 조건: `.pdf` 1개 이상
- 문서 내부 검사 조건:
  - 파일 존재 여부만 자동 판정한다(내용 검사 없음).
  - 찾은 PDF의 1페이지를 이미지 산출물로 저장해 웹페이지 버튼으로 조회할 수 있게 한다.
- 변수 사용 목록: 없음
- 실패 메시지:
  - 파일 누락: `시험기록서 파일을 찾을 수 없습니다`
  - 확장자 오류: `시험기록서 파일이 pdf가 아닙니다`
- 저장 산출물: PDF 1페이지 캡처 이미지
- 산출물 결과 키: `시험기록서`
- 구현 메모:
  - 내용 검사 없이 존재 여부만 판정하는 수동 검토 대상이다.
  - `downloadable_artifact_check`는 기본적으로 PDF 원본 다운로드를 제공할 수 있지만, 14번은 `artifact_first_page=True` 설정으로 1페이지 캡처 이미지를 제공한다.

## 15. 품질검사표

- 상태: `구현됨`
- code: `artifact_16`
- rule_type: `quality_inspection_table_check`
- 점검 대상 폴더: `인증관련` 단어가 포함된 폴더 (실제 다운로드 결과에서 `5.인증관련`처럼 최상위에 올 수 있어 `시험` 하위로 한정하지 않는다)
- 파일명 조건: `품질검사표`, `{프로젝트번호}` 포함
- 확장자/개수 조건: Excel 파일 전체 허용. `.xls` 또는 `.xlsx`, 정확히 1개
- 문서 내부 검사 조건:
  - Excel 파일에는 `{프로젝트번호} 품질검사표` 시트만 존재해야 한다.
  - `{측정항목별점수표}` 값과 D4~D87 값이 동일해야 한다.
  - 비교 값이 양쪽 모두 숫자로 해석 가능하면 숫자값으로 비교한다. 예: `1`, `1.0`, `1.00`은 동일하다.
  - E4부터 E85까지의 값을 확인한다.
  - 병합된 셀이 포함되어 있어 실제 산출 값 개수는 33개로 본다.
  - 33개 값 중 27번째 값은 품질평가보고서에 없는 값이므로 비교용 산출값에서 제외한다.
  - 나머지 값을 `4~26, 28~33, 1~3` 순서가 되도록 재정렬해 `{품질부특성측정값}` 변수에 저장한다.
- 변수 사용 목록: `{프로젝트번호}`, `{측정항목별점수표}`, `{품질부특성측정값}`
- 실패 메시지:
  - 파일 누락: `품질검사표 파일 확인 불가`
  - 시트명 오류: `품질검사표 시트명 확인 필요`
  - 측정항목별 점수표 불일치: `측정항목별 점수표가 점검표와 상이함`
- 저장 산출물:
  - D4~D87 비교 실제/기대값
  - `{품질부특성측정값}` 산출값과 원본/재정렬 순서
- 산출물 결과 키: `품질검사표`
- 구현 메모:
  - `{측정항목별점수표}`는 12번 점검표 규칙에서 산출한다.
  - `{품질부특성측정값}`은 E4:E85에서 실제 값 33개를 추출한 뒤 27번째 값을 제외하고 `4~26, 28~33, 1~3` 순서로 재정렬해 저장한다.
  - `{품질부특성측정값}`은 16번 품질평가보고서의 품질특성별 세부 평가결과 비교에 사용한다.

## 16. 품질평가보고서

- 상태: `구현됨`
- code: `artifact_15`
- rule_type: `quality_evaluation_report_check`
- 점검 대상 폴더: `인증관련` 단어가 포함된 폴더 (실제 다운로드 결과에서 `5.인증관련`처럼 최상위에 올 수 있어 `시험` 하위로 한정하지 않는다)
- 파일명 조건: `품질평가보고서`, `{프로젝트번호}` 포함
- 확장자/개수 조건: Word 파일(`.docx`, `.docm`, `.doc`) 정확히 1개
- 문서 내부 검사 조건:
  - 파일 내 `{프로젝트번호}` 검색 개수가 6개여야 한다.
  - 파일 내 `성  명 : 김  성  희` 값이 있어야 한다.
  - 파일 내 `정  성  룡     (서명)` 값이 있어야 한다.
  - `회사(기관)명` 값이 작성된 표의 셀을 찾고, 오른쪽 셀 값이 `{회사명}`(국문명) 또는 영문명 병기값(있는 경우) 중 하나와 일치해야 한다.
  - `신청일자 : yyyy년 m월 d일` 값을 찾아 날짜가 `{신청일}`과 동일해야 한다.
  - `계약일자 : yyyy년 m월 d일` 값을 찾아 날짜가 `{계약일}`과 동일해야 한다.
  - `제품시험평가 : yyyy년 m월 d일 ~ yyyy년 m월 d일` 값을 찾아 날짜가 `{시작일} ~ {종료일}`과 동일해야 한다.
  - `품질인증심의위원회 : yyyy년 m월 d일` 값을 찾아 날짜가 `{인증위}`와 동일해야 한다.
  - `<품질특성별 세부 평가결과>` 문장은 목차에도 포함될 수 있으므로 표 탐색 기준으로 사용하지 않는다.
  - 문서의 마지막 표부터 역순으로 확인하여 1행 1열에 `품질특성` 단어가 포함된 표를 찾는다.
  - 해당 표 1행 3열의 `평가결과` 값을 찾고, 그 아래 3열 값들을 `{품질부특성측정값}`과 비교한다.
  - 비교 값이 양쪽 모두 숫자로 해석 가능하면 숫자값으로 비교한다. 예: `1`, `1.0`, `1.00`은 동일하다.
  - 숫자가 아닌 `NA` 또는 `N/A`가 표시된 경우 오른쪽 칸에 `해당사항 없음` 값이 있어야 한다.
- 변수 사용 목록: `{프로젝트번호}`, `{회사명}`, `{신청일}`, `{계약일}`, `{시작일}`, `{종료일}`, `{인증위}`, `{품질부특성측정값}`
- 실패 메시지:
  - 파일 누락: `품질평가보고서 파일 확인 불가`
  - 프로젝트 번호 개수 오류: `프로젝트 번호 확인 필요`
  - 서명란 이름 오류: `서명란 이름 확인 필요`
  - 회사명 표 오류: `1. 신청 회사 현황 표 값 확인 필요`
  - 신청일 오류: `신청일자가 잘못 작성됨`
  - 계약일 오류: `계약일자가 잘못 작성됨`
  - 시험기간 오류: `시험기간이 잘못 작성됨`
  - 인증위 날짜 오류: `인증위 날짜가 잘못 작성됨`
  - 품질부특성 측정값 불일치: `품질검사표의 품질부특성 측정값과 상이함`
  - NA 설명 오류: `NA 해당사항 없음 작성 오류`
- 저장 산출물:
  - 프로젝트번호 검색 위치/개수
  - 날짜 비교 실제/기대값
  - 품질부특성 측정값 비교 결과, 총 비교 개수, 불일치 개수
- 산출물 결과 키: `품질평가보고서`
- 구현 메모:
  - `{신청일}`은 `reference_project.request_date`, `{계약일}`은 `reference_project.contract_date`, `{인증위}`는 `reference_project.cert_date` 또는 `cert_committee_date`에서 가져온다.
  - `{품질부특성측정값}`은 15번 품질검사표 규칙 산출값과 연결한다.
  - `김성희`, `정성룡`은 향후 변경 가능성이 있으면 규칙 JSON expected 값으로 둔다.
  - 15번 품질검사표 실제 규칙의 `sort_order`는 145로 고정되어, 16번 품질평가보고서의 기본 `sort_order=150`보다 먼저 실행된다.

## 17. SW저작권확인서

- 상태: `구현됨`
- code: `artifact_17`
- rule_type: `required_artifact_file`
- 점검 대상 폴더: `인증관련` 단어가 포함된 폴더
- 파일명 조건: `확인서` 포함
- 확장자/개수 조건: `.pdf` 1개 이상
- 문서 내부 검사 조건: 없음
- 변수 사용 목록: 없음
- 실패 메시지:
  - 폴더 누락: `인증관련 폴더를 찾을 수 없습니다`
  - 파일 누락: `저작권확인서 파일을 찾을 수 없습니다`
  - 확장자 오류: `저작권확인서 파일이 pdf가 아닙니다`
- 저장 산출물: 매칭된 PDF 파일명/경로
- 산출물 결과 키: `SW저작권확인서`
- 구현 메모: 파일명에 `{프로젝트번호}` 포함 여부는 요구하지 않는다.

## 18. 홍보이미지

- 상태: `구현됨`
- code: `artifact_18`
- rule_type: `promotional_material_check`
- 점검 대상 폴더: `홍보` 단어가 포함된 폴더
- 파일명 조건: 없음
- 확장자/개수 조건: 다음 둘 중 하나만 만족하면 적합(OR 조건)
  - 한글 파일(`.hwp`, `.hwpx`) 또는 워드 파일(`.doc`, `.docx`, `.docm`)이 있고, 그 파일명에 `예시`가 포함되지 않는다.
  - 이미지 파일(`.png`, `.jpg`, `.jpeg`, `.bmp`, `.gif`)이 1개 이상 있다. 이미지 파일은 파일명에 `예시`가 있어도 금지어 검사를 하지 않는다.
- 문서 내부 검사 조건: 없음
- 변수 사용 목록: 없음
- 실패 메시지:
  - 폴더 누락: `홍보 폴더를 찾을 수 없습니다`
  - 한글/워드/이미지 파일 모두 없음: `홍보이미지 파일을 찾을 수 없습니다`
  - 한글/워드 파일만 있고 파일명에 예시 포함(이미지도 없음): `홍보이미지 파일명에 '예시'가 포함되어 있습니다.`
- 저장 산출물: 매칭된 한글/워드/이미지 파일명 목록
- 산출물 결과 키: `홍보이미지`
- 구현 메모:
  - 한글/워드 조건과 이미지 조건은 서로 독립적인 OR이다. 한글/워드 파일명에 `예시`가 있어도 이미지 파일이 1개 이상 있으면 적합 처리한다.
  - 이미지 파일 쪽은 파일명 금지어(`예시`) 검사를 하지 않는다 — 한글/워드 파일만 검사 대상이다.
  - `promotional_material_check`는 `_evaluate_promotional_material_check`(`gscert_review_core/engine.py`)에서 구현하며, `required_artifact_file`과는 별개의 전용 rule_type이다.

## 연결 예정 항목

| 항목 | 상태 | 메모 |
| --- | --- | --- |
| 9번 시험계획서 `<세부사양>` 표 비교 | 구현됨 | 8번 시험성적서가 산출한 `시험성적서_세부사양표`와 9번 시험계획서의 `<세부사양>` 다음 표를 셀 공백/줄바꿈 제거 후 완전 일치 비교한다. |
| 10번 결함리포트 파일 개수/보고일자 검사 | 구현됨 | 8번 시험성적서에서 `{결함차수}`, `{1차}`, `{2차}` 등 차수별 보고일자를 산출하고, 10번 결함리포트가 해당 값을 읽어 파일 개수/버전/보고일자를 검사한다. |
| 11번 테스트케이스 잔여 결함 개수 비교 | 구현됨 | 10번 결함리포트 규칙에서 산출한 `{잔여결함수}`를 11번 테스트케이스가 읽어 `상세 테스트 결과` 열의 `F` 개수와 비교한다. |
| 12번 점검표 신뢰성 결함 개수 비교 | 구현됨 | 10번 결함리포트 규칙에서 산출한 `{H}`, `{R}`을 12번 점검표가 읽어 연결한다. |
| Google Sheet H/I열 동기화 | 구현됨 | `sync_reference_projects_from_sheet`가 H열을 `{신청일}`, I열을 `{계약일}`로 `reference_project`에 저장한다. |
| 16번 품질평가보고서 품질부특성 측정값 비교 | 구현됨 | 15번 품질검사표에서 산출한 `{품질부특성측정값}`을 16번 품질평가보고서가 읽어 연결한다. |
| 15번 품질검사표 측정항목별 점수표 비교 | 구현됨 | 12번 점검표에서 산출한 `{측정항목별점수표}`를 15번 품질검사표가 읽어 D4~D87과 비교한다. |
| PDF/Excel 캡처 조회 | 구현됨 | PDF 1페이지 PNG와 Excel 영역 표 이미지 저장/조회 API가 구현됨. |

## rule_type 참고

현재 구현된 주요 `rule_type`은 다음과 같다.

| rule_type | 용도 |
| --- | --- |
| `required_artifact_file` | 폴더, 파일명, 확장자, 개수 조건만 검사 |
| `downloadable_artifact_check` | 파일 존재만 판정하고 PDF 원본 다운로드 또는 1페이지 캡처 산출물을 제공 |
| `document_artifact_check` | 파일 조건과 docx/pdf 내부 조건을 함께 검사 |
| `excel_feature_list_check` | 기능리스트 Excel 내부값과 표 영역 캡처 검사 |
| `test_plan_document_check` | 시험계획서 docx/pdf 내부값, 바닥글 필수/금지어, 세부사양 표 비교 검사 |
| `image_screenshot_folder_date_check` | 제품 스크린샷 폴더 후보와 이미지 수정일자 검사 |
| `test_case_check` | 테스트케이스 Excel 내부값, 바닥글 금지어, 잔여 결함 F 개수 검사 |
| `defect_report_check` | 결함리포트 버전/시트/머리글/바닥글/보고일자 검사와 `{잔여결함수}`, `{H}`, `{R}` 산출 |
| `inspection_checklist_check` | 점검표 Excel/PDF, 머리글/바닥글, 표 내용 검사와 `{측정항목별점수표}` 산출 |
| `rawdata_folder_structure_check` | 결함리포트/보안/성능 rawdata 폴더 구조 검사 |
| `test_report_document_check` | 시험성적서 docx/pdf 검사와 결함 차수/차시 변수 산출 |
| `quality_inspection_table_check` | 품질검사표 Excel 검사와 `{품질부특성측정값}` 산출 |
| `quality_evaluation_report_check` | 품질평가보고서 docx 검사와 품질검사표 산출값 비교 |
| `promotional_material_check` | 홍보이미지: 한글/워드 파일(파일명 금지어 검사) 또는 이미지 파일(금지어 검사 없음) 중 하나만 있으면 적합(OR) |

새 규칙이 기존 `rule_type`으로 표현하기 어려우면 `ecm_download_review_inspection.py`에 작은 검사 유형을 추가한다. 복잡한 로직을 worker에 직접 넣지 않는다.

현재 공유 엔진 버전은 `0.2.0`이고 rulebase manifest의 `engine_min_version`도 `0.2.0`이다. 로컬 앱은 이 값을 비교해 오래된 엔진으로 신규 규칙셋을 실행하지 않도록 막는다.

## 수정 절차

1. 새 규칙 요구사항을 받으면 애매한 부분을 질문해 확정한다.
2. 확정된 조건을 이 문서에 먼저 추가한다.
3. 구현 전 상태는 `정의 확정` 또는 `정의 중`으로 둔다.
4. 코드와 seed에 반영한 뒤 상태를 `구현됨`으로 변경한다.
5. 규칙 결과가 `reference_project.artifact_results_json`과 레거시 `ecm_list` 호환 경로에서 어떤 산출물 키로 저장되는지 반드시 적는다.
6. 다음 PC에서 이어서 작업할 수 있도록 `main/docs/00_next_step.md`에는 바로 다음 작업만 요약한다.

## 검증 명령

현재 활성 규칙 확인:

```powershell
.\.venv\Scripts\python.exe manage.py shell --settings=myproject.ui_mock_settings -c "from main.models import DownloadReviewRule; import json; [print(r.code, r.name, r.rule_type, r.enabled, json.dumps(r.config_json, ensure_ascii=False)) for r in DownloadReviewRule.objects.order_by('sort_order')]"
```

실제 규칙 seed dry-run:

```powershell
.\.venv\Scripts\python.exe manage.py seed_download_review_rules --only-real --dry-run --settings=myproject.ui_mock_settings
```

문서 핵심 항목 확인:

```powershell
rg "상태:|기능리스트|시험계획서|Copyright \{연도\} TTA|\{시작일\}|\{버전\}" main/docs/03_inspection_rule_manual.md
```
