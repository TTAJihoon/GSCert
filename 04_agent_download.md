# 에이전트 다운로드 설계

## 에이전트 정보

작업 관리자에서 확인된 앱 이름:

- `DestinyECMAgent(32비트)`

다운로드 중 표시되는 창:

- `전송현황`

중복 파일 다운로드 시 표시되는 창:

- `시스템 알림`

## 폴더 선택 팝업

파일 다운로드 메뉴 실행 후 Windows 팝업이 표시된다.

창 제목:

- `폴더 찾아보기`

화면 문구:

- `디렉토리 선택`

기본 경로:

- 개발 PC 기준: `C:\Users\jh910\Downloads`
- 실사용 서버에서는 설정값으로 변경 가능해야 한다.

## 경로 설정 정책

다운로드 기본 경로는 코드에 고정하지 않는다.

환경별로 변경할 수 있도록 설정값으로 관리한다.

추천 설정명:

```text
AGENT_DOWNLOAD_BASE_DIR
```

개발 PC 기본값:

```text
C:\Users\jh910\Downloads
```

서버 운영 시 예:

```text
D:\GSCertAgentDownloads
```

설정 우선순위:

1. 환경변수 `AGENT_DOWNLOAD_BASE_DIR`
2. Django settings 값 `AGENT_DOWNLOAD_BASE_DIR`
3. Windows 사용자 다운로드 폴더

운영 서버에서는 1번 또는 2번 방식으로 명시 설정한다.

사용자는 폴더 트리를 클릭해야 한다.

## 다운로드 폴더 정책

zip 파일명은 프로젝트 폴더명과 동일하다.

중복 다운로드 문제를 줄이기 위해 프로젝트별 고유 폴더 사용을 우선 설계한다.

예:

```text
Downloads/GSCert/{작업ID}/{프로젝트번호}
```

폴더 트리 자동화가 불안정하면 아래처럼 더 단순한 구조를 검토한다.

```text
Downloads/{프로젝트번호}
```

실제 경로는 `AGENT_DOWNLOAD_BASE_DIR` 아래에 생성한다.

예:

```text
{AGENT_DOWNLOAD_BASE_DIR}\GSCert\{작업ID}\{프로젝트번호}
```

## 다운로드 완료 판단

1. `전송현황` 창이 뜨는지 감지한다.
2. `전송현황` 창이 사라질 때까지 대기한다.
3. 선택한 다운로드 폴더에서 프로젝트 폴더명과 동일한 zip 파일을 찾는다.
4. zip 파일 크기가 일정 시간 동안 변하지 않는지 확인한다.
5. zip 파일을 열 수 있는지 확인한다.

## 중복 파일 처리

중복 파일로 `시스템 알림` 창이 뜨면 아래 순서로 처리한다.

1. 시스템 알림 창 내용을 기록한다.
2. 알림을 닫는다.
3. 새 다운로드 폴더명을 생성한다.
4. 폴더 변경 후 같은 프로젝트 다운로드를 재시도한다.
5. 재시도 횟수를 초과하면 해당 프로젝트를 실패 처리한다.
6. 다음 프로젝트로 진행한다.

## 테스트로 검증할 항목

- 폴더 찾아보기 창을 `pywinauto`로 안정적으로 찾을 수 있는지
- 다운로드 폴더를 트리 클릭으로 선택할 수 있는지
- 새 폴더 만들기 버튼을 자동화할지, 서버가 미리 만든 폴더를 선택할지
- 전송현황 창이 사라지는 시점과 실제 zip 생성 완료 시점이 일치하는지
- 시스템 알림 창을 닫은 뒤 재시도가 가능한지

## 폴더 선택 팝업 검증 시나리오

1. 서버가 Windows 다운로드 폴더 아래에 테스트 폴더를 미리 만든다.
2. 웹페이지1에서 파일 다운로드 메뉴를 실행한다.
3. `폴더 찾아보기` 창이 표시되는지 확인한다.
4. 트리에서 `다운로드` 폴더를 찾을 수 있는지 확인한다.
5. 테스트 폴더를 선택할 수 있는지 확인한다.
6. `확인` 버튼을 클릭할 수 있는지 확인한다.
7. `전송현황` 창이 표시되는지 확인한다.
8. `전송현황` 창이 사라질 때까지 기다린다.
9. 테스트 폴더에 zip 파일이 생성되었는지 확인한다.
10. zip 파일명이 프로젝트 폴더명과 동일한지 확인한다.

## 폴더 선택 자동화 후보

우선순위 1: 미리 생성한 단순 경로 선택

```text
Downloads/GSCert/TTA-26-00009
```

설정 기반 실제 경로:

```text
{AGENT_DOWNLOAD_BASE_DIR}\GSCert\TTA-26-00009
```

우선순위 2: 중복 발생 시 suffix 폴더 선택

```text
Downloads/GSCert/TTA-26-00009_2
```

우선순위 3: 트리 자동화가 불안정하면 기본 다운로드 폴더를 사용하고, 중복 발생 시 실패 이력 후 다음 프로젝트로 진행하는 임시 정책을 검토한다.
