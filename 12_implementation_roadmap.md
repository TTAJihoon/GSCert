# 구현 로드맵

## 목적

설계가 확정된 부분부터 안전하게 구현하기 위한 순서와 완료 기준을 정리한다.

검사 규칙 상세 구현은 마지막 단계로 보류한다.

## 구현 원칙

- 한 번에 큰 기능을 모두 만들지 않고 단계별로 검증한다.
- 각 단계가 끝날 때 관련 루트 설계 문서와 폴더별 `readme.md`를 갱신한다.
- Windows 에이전트 자동화는 실제 서버 또는 동일 조건의 PC에서 테스트하며 보정한다.
- 기준 DB와 실행 이력 DB는 분리한다.
- worker는 Uvicorn과 별도 프로세스로 실행한다.

## 0단계: mock 데이터 기반 UI 목업

목표:

- 실제 DB/API 구현 전에 사용자가 화면 구성과 업무 동선을 검토할 수 있게 한다.
- 프로젝트 선택, 진행상황, 결과 조회 화면을 mock 데이터로 구현한다.
- active job, worker heartbeat 지연, 실패 프로젝트 등 주요 상태를 화면에서 확인한다.

완료 기준:

- 주요 화면 3개를 탭으로 전환할 수 있다.
- 프로젝트 선택과 작업 요청 버튼의 활성/비활성 상태를 확인할 수 있다.
- 진행상황과 결과 조회에 정상/실패/재시도 사례가 표시된다.
- 사용자가 UI 디자인과 동선을 검토하고 수정 의견을 줄 수 있다.

관련 문서:

- `13_ui_mockup_design.md`
- `08_ui_api_design.md`

## 1단계: 프로젝트 기준 DB 조회

목표:

- `main/data/reference.db`의 `ecm` 테이블을 읽는다.
- 프로젝트 목록 API를 만든다.
- 프로젝트 선택 화면에서 목록을 표시한다.

완료 기준:

- `프로젝트번호`, `인증일자`, `회사명`, `제품명`, `시험PL`, `점검결과`를 화면에서 확인할 수 있다.
- 검색 또는 필터 없이도 기본 목록 조회가 가능하다.
- DB 파일이 없거나 테이블이 없을 때 사용자에게 이해 가능한 오류를 보여준다.

관련 문서:

- `02_database_design.md`
- `08_ui_api_design.md`

## 2단계: 작업 생성, 락, 상태 저장

목표:

- `workflow.db`를 만든다.
- 작업 테이블, 프로젝트별 작업 테이블, 락 테이블을 만든다.
- 사용자가 선택한 프로젝트 목록으로 작업을 생성한다.
- 실행 중 작업이 있으면 새 작업을 거절한다.

완료 기준:

- 새 작업 요청 시 DB에 작업과 프로젝트별 작업이 저장된다.
- 실행 중 작업이 있을 때 두 번째 요청은 거절된다.
- 브라우저를 닫았다가 다시 열어도 현재 작업 상태를 조회할 수 있다.

관련 문서:

- `02_database_design.md`
- `06_recovery_and_lock.md`
- `08_ui_api_design.md`

## 3단계: worker 프로세스 기본 골격

목표:

- `run_download_worker` management command를 만든다.
- worker가 pending 작업을 찾고 running으로 변경한다.
- heartbeat를 저장한다.
- 프로젝트별 상태를 순차적으로 변경하는 dry-run을 만든다.

이 단계에서는 실제 웹페이지1/에이전트 자동화를 실행하지 않는다.

완료 기준:

- worker를 별도 프로세스로 실행할 수 있다.
- pending 작업이 running, completed로 상태 전이된다.
- heartbeat가 주기적으로 갱신된다.
- worker 중단 후 stale 작업을 식별할 수 있다.

관련 문서:

- `09_worker_process_design.md`
- `10_operations_scripts.md`

## 4단계: 운영 스크립트 작성

목표:

- `start_worker.ps1`
- `stop_worker.ps1`
- `start_all.ps1`
- `stop_all.ps1`
- `status.ps1`

을 작성한다.

완료 기준:

- Uvicorn과 worker를 각각 시작/중지할 수 있다.
- 통합 스크립트로 둘을 함께 시작/중지할 수 있다.
- `status.ps1`에서 서버 PID, worker PID, 포트, heartbeat를 확인할 수 있다.

관련 문서:

- `10_operations_scripts.md`

## 5단계: 웹페이지1 자동화 dry-run

목표:

- 웹페이지1에 접속한다.
- 프로젝트번호로 폴더를 찾는다.
- 프로젝트 폴더를 클릭한다.
- 문서 목록 전체 선택까지 수행한다.

이 단계에서는 파일 다운로드 메뉴 클릭 전까지만 검증한다.

완료 기준:

- `TTA-26-00009` 형식의 프로젝트번호로 정확한 폴더를 선택한다.
- 위치 기반 selector 없이 텍스트/속성 기반으로 폴더를 찾는다.
- 문서 목록 전체 선택 체크박스를 안정적으로 클릭한다.

관련 문서:

- `03_webpage1_automation.md`

## 6단계: 파일 다운로드 메뉴 실행 검증

목표:

- `#menu-folder-list-drop` 버튼을 클릭한다.
- `menuevent="saveDocumentsFileAll"` 메뉴 항목을 클릭한다.
- Windows `폴더 찾아보기` 창이 뜨는지 확인한다.

완료 기준:

- 메뉴 표시와 파일 다운로드 항목 클릭이 안정적으로 동작한다.
- 폴더 선택 팝업이 표시되는 시점을 감지할 수 있다.
- 문서 목록이 비어 있을 때 실패 이력을 남긴다.

관련 문서:

- `03_webpage1_automation.md`
- `04_agent_download.md`

## 7단계: 폴더 선택 팝업 자동화

목표:

- `AGENT_DOWNLOAD_BASE_DIR` 아래 프로젝트별 폴더를 만든다.
- Windows `폴더 찾아보기` 창에서 해당 폴더를 선택한다.
- 확인 버튼을 클릭한다.

완료 기준:

- 개발 PC 기준 `C:\Users\jh910\Downloads` 아래 테스트 폴더 선택이 가능하다.
- 서버에서는 설정값으로 다운로드 기준 경로를 바꿀 수 있다.
- 폴더 트리 자동화가 실패하면 명확한 실패 이력을 남긴다.

관련 문서:

- `04_agent_download.md`

## 8단계: 전송현황/시스템 알림 처리

목표:

- `전송현황` 창을 감지한다.
- 창이 사라질 때까지 대기한다.
- `시스템 알림` 창이 뜨면 폴더명을 변경하고 재시도한다.

완료 기준:

- 정상 다운로드 완료를 감지할 수 있다.
- 중복 파일 알림 발생 시 폴더 변경 후 재시도한다.
- 재시도 횟수 초과 시 프로젝트 실패 처리 후 다음 프로젝트로 진행한다.

관련 문서:

- `04_agent_download.md`
- `06_recovery_and_lock.md`

## 9단계: zip 파일 확인 및 프로젝트별 다운로드 결과 저장

목표:

- 다운로드 폴더에서 프로젝트 폴더명과 동일한 zip 파일을 찾는다.
- zip 파일 안정화와 열기 가능 여부를 확인한다.
- 프로젝트별 zip 경로를 DB에 저장한다.

완료 기준:

- zip 파일이 없으면 프로젝트 실패로 기록한다.
- zip 파일이 손상되었으면 프로젝트 실패로 기록한다.
- 정상 zip은 프로젝트 상태를 downloaded 또는 completed_download로 저장한다.

관련 문서:

- `04_agent_download.md`
- `05_zip_inspection.md`

## 10단계: 결과 조회 화면

목표:

- 완료 작업 목록을 조회한다.
- 프로젝트별 다운로드 성공/실패 이력을 조회한다.
- 실패 단계와 메시지를 확인할 수 있다.

완료 기준:

- 사용자가 브라우저를 닫았다가 다시 열어도 결과를 볼 수 있다.
- 실패 프로젝트만 필터링할 수 있다.
- zip 경로와 실패 메시지를 확인할 수 있다.

관련 문서:

- `08_ui_api_design.md`

## 11단계: 검사 규칙 설계 및 구현

목표:

- Word, Excel, PDF, 파일명, 저장시간 검사 규칙을 하나씩 추가한다.
- 규칙별 결과를 최소 단위로 저장한다.

이 단계는 현재 보류한다.

관련 문서:

- `05_zip_inspection.md`
- `07_skill_strategy.md`

## 우선 구현 대상

처음 구현할 범위는 0단계 UI 목업이다.

UI 목업 검토가 끝난 뒤 1단계부터 4단계까지 진행한다.

1~4단계가 끝나면 실제 에이전트 자동화 없이도 아래 흐름을 검증할 수 있다.

```text
프로젝트 목록 조회
→ 작업 생성
→ 단일 작업 락
→ worker dry-run 처리
→ 진행상황 조회
→ 결과 조회
```

그 다음 5단계부터 실제 웹페이지1/에이전트 자동화를 붙인다.

## 구현 전 확인할 것

- `reference.db` 샘플 파일 준비 여부
- `ecm` 테이블 생성 방식
- 운영 서버에서 worker를 창 표시 상태로 테스트할 수 있는지
- 현재 브랜치에 설계 문서를 먼저 커밋할지 여부
